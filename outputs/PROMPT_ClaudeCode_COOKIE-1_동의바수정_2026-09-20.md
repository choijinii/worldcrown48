# Claude Code 전달 프롬프트 — COOKIE-1 (동의 기록 버그 + 동의 바 재배치) · **초안**

> **상태: 초안. 대표 승인 전에는 실행하지 않는다.** §9 승인 게이트 2건(동의 바 한 줄 56px · 모바일 가로 비표시)이 열려 있고, 둘 다 **법무 성격**이라 티오·클로드 코드가 임의로 정할 수 없다.
> 작성 2026-09-20 (ARENA-1 PR 1 운영 스모크에서 발견) · 담당 예정 Claude Code · 범위 확정 대표 2026-09-20
> 정본 우선순위: **① 결정 원장 `outputs/DECISIONS_결정원장_v1.0_2026-09-11.md` > ② 이 프롬프트 > ③ 기타 문서**

---

## §0 목적 / 완료 정의(DoD)

**목적**: 쿠키 동의 층의 결함 두 개(기록이 영원히 안 읽힘 · es 팬은 동의 저장 자체가 거부됨)를 고치고, 동의 바가 아레나 무대와 배너 자리를 덮지 않게 한다.

**DoD**
- [ ] 동의를 한 번 누른 팬은 **다음 방문에 배너가 뜨지 않는다** (지금은 매번 뜬다).
- [ ] **es 팬이 동의를 저장할 수 있다** (지금은 규칙이 거부한다).
- [ ] 동의 바가 **데스크톱 1440×900에서 무대 아래 배너 자리를 가리지 않는다**.
- [ ] **모바일 가로(844×390)에서 동의 바가 무대를 덮지 않는다** (§9 게이트 2의 결론대로).
- [ ] 동의 바·모달의 **팬 노출 문구는 한 글자도 바뀌지 않는다** (§4 R1 · grep + 스냅샷 테스트로 판정).
- [ ] 신설 테스트: `lib/__tests__/policy/cookieConsent.test.ts`(순수) · `tests/rules/cookie-consent.rules.test.ts`(에뮬레이터) — **현재 이 층에는 유닛·규칙 테스트가 0건이다.**
- [ ] `npx vitest run` · `npx tsc --noEmit` · `npm run check:hex` · `cd functions && npm run build` green, Console 에러 0.
- [ ] 최종 보고에 "핸드오프와 달랐던 점".

## §1 필독 (이 순서로)

1. `outputs/DECISIONS_결정원장_v1.0_2026-09-11.md` — **D-17 ③**(모바일 가로 = 집중 모드, 상단 메뉴를 뺀다) · **D-21 바뀜 2026-09-19**(모바일 가로에는 배너를 두지 않는다). 동의 바를 가로에서 빼자는 발상의 뿌리가 이 둘이다. "아니라고 한 것"을 반드시 읽어라.
2. `lib/cookieConsent.ts` — 순수 층. `CURRENT_POLICY_VERSION`(84) · `writeConsentBreadcrumbCookie`(174) · `readConsentBreadcrumbCookie`(185) · `Lang`(54).
3. `components/policy/CookieConsentProvider.tsx` — 부팅 경로(흔적 쿠키 → Firestore → 배너). 익명 로그인(`ensureAnonymousUid`)이 여기서 일어난다.
4. `components/policy/CookieBanner.tsx` — 3블록 문구(ko 제목·ko 본문·en 본문) + 버튼 3개. **문구는 손대지 않는다.**
5. `app/globals.css` 494~ (`.cookie-banner` · `.cb-inner` · `.cb-*`) · 881~ (모바일 분기).
6. `firestore.rules` 103~150 `cookieConsents` 블록 — 스키마 검증.
7. `docs/lite-specs/E1-policy-hub.md` — 정책 도메인 스펙(문구 권위).

## §2 실측 (2026-09-20 · main `7bb4a93` 운영 스모크)

| # | 현상 | 실물 |
|---|---|---|
| 1 | **흔적 쿠키가 한 번도 읽히지 않는다** | 값은 `"1.0.<ms>"`인데 읽을 때 `raw.split(".")` → version이 `"1"`. `CURRENT_POLICY_VERSION`은 `"1.0"` → 언제나 불일치 → `null`. 결과: 동의한 팬도 매 방문 Firestore를 조회하고, 조회 실패·지연이면 배너가 다시 뜬다 |
| 2 | **es 팬은 동의를 저장할 수 없다** | 규칙 143행 `lang == 'ko' || lang == 'en'`. `Lang` 타입에는 `es`가 있어 클라이언트는 `lang: "es"`를 보낸다 → permission-denied |
| 3 | **동의 바가 배너 자리를 덮는다** | 1440×900에서 페이지가 약 266px만 스크롤돼 배너가 y 694–846에 놓이고, 하단 고정 동의 바(약 200px)가 그 위를 덮는다. 첫 방문자는 배너를 볼 수 없다 |
| 4 | **모바일 가로에서 무대 전체를 덮는다** | 844×390에서 동의 바가 화면을 가득 채워 무대가 보이지 않고 탭도 가로챈다(ARENA-1 E2E가 배너를 닫고 시작하는 이유) |
| — | 테스트 공백 | `cookieConsents` 규칙 테스트 **0건**, `lib/cookieConsent.ts` 유닛 테스트 **0건**. `e2e/d1-auth.spec.ts:160`이 문서 존재만 확인한다 |

## §3 스코프

| | 내용 |
|---|---|
| **IN** | ① 흔적 쿠키 버전 파싱 수정 ② `firestore.rules` `cookieConsents`에 `es` 추가 + 규칙 테스트 신설 ③ 동의 바를 **한 줄 높이 56px**로 재배치(§9 게이트 1) ④ **모바일 가로에서 동의 바 비표시**(§9 게이트 2) + 신설 유닛·규칙 테스트 |
| **OUT** | 동의 **문구** 변경(R1) · 동의 카테고리·모달 구조 변경 · 개인정보처리방침/쿠키 정책 본문 · `ipHash` 콜러블 · 익명 로그인 시점 변경 · 배너 자리(BannerSlot)·무대(ARENA-1) 재작업 · 동의 기록 마이그레이션 |

## §4 RULE

- **R1 문구 불변** — 배너·모달의 팬 노출 문구는 한 글자도 바꾸지 않는다. 법무 승인 문안이다. 줄바꿈·요약·"자세히" 접기로 **보이는 문장이 줄어드는 것도 변경**이다(§9 게이트 1).
- **R2 동의 없이 선택 쿠키 금지** — 어떤 배치 변경도 "동의 전에는 필수 쿠키만"을 깨지 않는다. 가로에서 배너를 감추는 경우에도 동의는 **미루는 것**이지 **가정하는 것**이 아니다.
- **R3 추가만** — `cookieConsents` 스키마는 `es` 허용을 **추가**할 뿐, 기존 필드·검증을 빼지 않는다(옛 문서 그대로 유효).
- **R4 옛 쿠키 호환** — 이미 심어진 `"1.0.<ms>"` 쿠키를 고친 뒤에도 읽을 수 있어야 한다. 파싱만 고치고 형식은 바꾸지 않는 쪽이 기본안.
- **R5 동작 변화 명시** — 이 수정으로 **배너 노출 빈도가 줄어든다**(지금은 매번 → 앞으로는 1회). 이건 의도된 결과다. 계측·문구는 그대로.
- **R6 색·크기는 토큰만** — `var(--…)`, raw hex 금지(`npm run check:hex`).
- **R7 선택 엔진·무대 불변** — ARENA-1 산출물(`SplitStage`·`BannerSlot`·`stageLayout`)은 건드리지 않는다.

## §5 Phase 분할 (Phase = 커밋 1개 · TDD RED→GREEN)

- **A 흔적 쿠키 파싱** — `lib/__tests__/policy/cookieConsent.test.ts` 신설: 쓰기→읽기 왕복 · `"1.0.<ms>"` 읽힘 · 정책 버전이 올라가면 무효 · 만료 · 손상된 값. → `readConsentBreadcrumbCookie` 수정(마지막 `.` 기준 분리 권장).
- **B 규칙 es** — `tests/rules/cookie-consent.rules.test.ts` 신설(`banners.rules.test.ts` 형태 그대로): 소유자만 읽기·쓰기 · ko/en/**es** 저장 성공 · 알 수 없는 lang 거부 · `essential:false` 거부 · 남의 uid 거부. → `firestore.rules` 143행에 `es` 추가.
- **C 동의 바 한 줄 56px** — §9 게이트 1 결론 확정 **후에만** 착수. 문구 불변 스냅샷 테스트를 먼저 만든다.
- **D 모바일 가로 비표시** — §9 게이트 2 결론 확정 **후에만** 착수. 판정은 순수 함수로 빼고(`shouldShowConsentBar({mode})`) 유닛 테스트, 화면은 얇은 렌더.
- **E 검증·PR** — §7 하네스 + E2E 회귀(`arena1-split-stage`는 배너를 닫고 시작하므로, D 반영 후 그 우회가 필요 없어지는지 확인하고 스펙을 정리).

## §6 Auto-STOP

- §9 게이트 1·2가 **미승인 상태에서 C·D에 손대는 것** → STOP.
- 문구를 한 글자라도 바꿔야 56px가 성립하는 상황 → STOP(게이트 1로 회부).
- 가로 비표시가 "동의 없이 선택 쿠키 사용"으로 귀결되는 설계 → STOP(R2 위반).
- 흔적 쿠키 형식 자체를 바꿔야 한다고 판단되는 경우 → STOP(R4).
- `firestore.rules` 배포 권한 필요 → STOP(사람 절차 · §9).
- 실물이 이 문서 §2와 다름 → STOP(근거 경로 + 권장안).

## §7 검증 하네스

| | 무엇 | 통과 기준 |
|---|---|---|
| 기계 | `npx vitest run` · `npx tsc --noEmit` · `npm run check:hex` · `cd functions && npm run build` | 전부 green |
| 기계 | `npm run test:rules` (에뮬레이터 · Java 21) | 신설 `cookie-consent.rules.test.ts` 포함 green |
| 기계 | 문구 스냅샷 — 배너·모달 문자열이 수정 전후 동일 | 차이 0 |
| 기계 | E2E 회귀: `d1-auth` · `arena1-split-stage` · `c1-arena-flow` | 100% PASS |
| 사람 | 프리뷰: 동의 → 새로고침 → **배너가 다시 뜨지 않는다** | 대표 확인 |
| 사람 | 프리뷰 1440×900: 무대 아래 배너 자리가 동의 바에 가리지 않는다 | 대표 확인 |
| 사람 | 프리뷰 844×390: 무대가 동의 바에 덮이지 않는다 | 대표 확인 |
| 사람 | es로 동의 저장 성공(콘솔 permission-denied 없음) | 대표 확인 |

## §8 PR 본문 4블록

A 무엇을 했나 · B 어떻게 확인했나(명령·실측) · C 판단이 필요했던 것 · D 핸드오프와 달랐던 점 · E 리뷰어 체크리스트(사용자 영향 한 줄: "동의한 팬에게 배너가 다시 뜨지 않고, es 팬이 동의를 저장할 수 있으며, 동의 바가 무대·배너를 가리지 않는다 — 동의 문구·카테고리 무변경").

## §9 승인 게이트 · 사람 절차

| # | 필요한 것 | 상태 |
|---|---|---|
| 1 | **동의 바 한 줄 56px** — 지금 배너는 ko 제목 + ko 본문 + en 본문 3블록이다. **문구를 그대로 두고 56px 한 줄에 담는 방법**(가로 배치? 스크롤? 본문을 링크 뒤로?)이 무엇인지, 혹은 문구 축약을 허용하는지 대표·법무 판단 필요. **R1과 정면으로 부딪힐 수 있다.** | ⏸ 열림 |
| 2 | **모바일 가로 비표시** — D-17 ③·D-21과 논리는 같지만, 동의 요청을 특정 화면에서 아예 안 보이게 하는 것이라 **법적 판단**이 필요하다. 기본안: 가로에서는 미루고(필수 쿠키만) 세로로 돌리면 보인다. | ⏸ 열림 |
| 3 | 동의 바 문구 변경 없음 확인 | ✅ 범위 확정(2026-09-20 대표) — 문구는 바꾸지 않는다 |
| — | `firebase deploy --only firestore:rules` (es 허용 반영) | 사람 절차 — 대표 |

## §10 참고 — 이 킥이 생긴 경위

ARENA-1 PR 1 운영 스모크(2026-09-20)에서 ①②③④가 한꺼번에 드러났다. ①②는 그 전부터 있던 결함이고(어제 PR 리뷰 때 ①을 보고), ③④는 무대·배너 자리가 생기면서 눈에 보이게 된 것이다. ARENA-1은 E2E에서 배너를 닫고 시작하는 우회로 통과했으므로, 이 킥이 끝나면 그 우회가 필요한지 다시 본다.

*초안 · 2026-09-20 · Claude Code (대표 범위 확정 반영, 승인 게이트 2건 열림)*
