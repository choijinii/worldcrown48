# 다음 세션 시작 프롬프트 (아레나 개편 · 디자인 1번 화면 검수)

작성 2026-09-12 09:50 KST · 앞 세션에서 이어짐

---

## ▼ 아래 블록을 새 세션 첫 메시지로 그대로 붙여 넣으세요

월클48(WorldCrown48) 이어서. 나는 대표, 너는 티오(T.O.)다.

**먼저 읽어라 (순서대로, 전부):**
1. 기억(메모리)의 `/preferences.md` — 특히 맨 아래 【기록 방식 개혁】 3줄
2. `outputs/DECISIONS_결정원장_v1.0_2026-09-11.md` — **확정 결정의 정본.** D-00~D-10. 이것과 다른 문서가 충돌하면 이 원장이 이긴다
3. `outputs/KICK_ARENA-1_v1.1_매치무대-대수술_2026-09-10.md` — 이번 작업의 킥 문서(확정 수치·측정치·M1~M9)
4. `outputs/ARENA-1_ClaudeDesign_설계요구_v1.1_2026-09-10.html` — 클로드 디자인에 넘긴 설계 요구서
5. `marketing/00_strategy/D-42_런칭로드맵_v1.1_2026-08-27.html` — 일정. **갱신하지 말 것**(D-10)

**지금 상태:**
- RUN-1은 PR 3까지 끝. PR #95·#96·#97 병합 완료. 현재 HEAD `05e5ac3` (main).
- ⚠️ `05e5ac3`(결정 원장 신설)은 **로컬 커밋만 되어 있고 origin에 push되지 않았다.** 세션 시작하면 push 상태를 확인해라. **main은 보호 브랜치라 직접 push가 안 된다 — 브랜치를 만들어 PR로 올려야 한다.**
  - 참고: 코워크의 명령 창은 연결된 폴더만 보이는 격리 공간이라 `~/.ssh`(깃허브 열쇠)가 없다. 원격 주소가 SSH(`git@github.com:...`)라서 코워크에서는 push가 되지 않는다. **고장이 아니라 원래 구조다.** push·PR·배포는 맥에서 직접 도는 클로드 코드의 역할.
- 저장소에 추적되지 않은 폴더 2개: `.claude/`, `marketing/`. 커밋할지 `.gitignore`에 넣을지 아직 결정 안 됨.
- 일정: W2(9/4~9/10) 아레나 매치 대수술이 목표였고 현재 **이틀 밀림**. W3(9/11~17)에 아레나 홈까지 만들어야 한다.

**이번 세션에 할 일:**
1. 클로드 디자인이 낸 **1번 화면(VS 스플릿 무대)** 결과를 내가 보여 주면, 킥 문서 v1.1의 확정 수치·금지 사항과 한 줄씩 대조해서 합격/불합격을 판정해라.
   - 특히 D-08(무대는 프레임 안 · 메뉴바 유지 · 좌우 50:50)과 D-07(스포츠 세계관 · 축구 컨셉 폐기), D-03(낱말 규칙) 위반이 있는지.
2. 합격하면 2번 화면 이후 브리프를 이어서 만든다. 불합격이면 무엇이 틀렸는지 표로 정리해서 재요청 브리프를 만든다.
3. W-7 폐기(D-04)를 아레나 개편 M9로 반드시 포함할 것. `firestore.rules`의 `ranking_cache` 읽기 조건에서 `tournamentDeadline < request.time`을 제거하는 일이다.

**규칙(재론 금지):**
- 확정 결정은 원장(D-00~D-10)을 인용해라. 다시 정의하지 마라.
- 새 결정은 **네 줄**로 기록해라 — 무엇 / 정의 / 왜 / **아니라고 한 것**. 수치는 의도를 먼저, 숫자를 뒤에, 그리고 "이 수치가 정하지 않는 것"을 덧붙여라.
- 세션 끝에 "제가 이해한 것" 5줄을 나에게 먼저 보여 주고, 확인받은 뒤에 문서·메모리에 적어라.
- 전문 용어·내부 번호는 처음 쓸 때 한 줄로 풀어 써라.
- 일정표를 갱신(재기준)하지 마라. 원래 날짜를 목표로 두고 밀린 만큼만 표시해라.

첫 줄에 날짜·시간(KST)부터 쓰고, 그다음 "지금 어디까지 와 있는지"를 요약해라.

## ▲ 여기까지

---

## 참고 · 열려 있는 항목 (급하지 않음)

| 항목 | 내용 |
|---|---|
| 스페인어 쿠키 동의 | `firestore.rules`의 `cookieConsents` 쓰기 규칙이 `lang == 'ko' || lang == 'en'`만 허용 — 스페인어 사용자는 동의창을 영구히 닫을 수 없음 |
| 다운로드 폴더 | `6 Firebase keys in the .jpg` (2026-06-14) 파일이 남아 있음 — 확인 후 폐기 권장 |
| 기사 템플릿 | `ARTICLE_TEMPLATES.md`는 저장소에 없음. 마케팅 채팅 안에만 있으므로 거기서 받아야 함 |
| Vercel | W4 클로즈드 베타 전에 Hobby → Pro 전환 필요 |
| T-4 이상 감지 | 12시간 주기에서 오탐 가능성 — 랭킹 개편(W4) 때 함께 볼 것 |
