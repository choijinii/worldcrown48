# 티오 다음 세션 시작 지시서 (2026-08-28 작성)

새 세션의 티오에게: 먼저 메모리에서 **user-communication-profile.md(대화 프로필)**과 **project-launch-v1_1-2026-08-27.md(런칭 일정 v1.1)**, **project-name-i18n-gap.md(이름 다국어 결정)**를 읽고 시작할 것. 말투·지도·최신 결정이 이 세 파일에 있다.

## 오늘(08-28)까지의 큰 변화
- **LAB-UX-1 공식 종료.** #76~#82 전부 머지·배포, 골든 재실행 PASS(로마자→한글 7건·지어낸 이름 0·기권 3건 동일 행·#6 소속 NMIXX는 근거 있는 개선), 대표 스모크에서 약속 기능 전부 확인(링크→빈칸, 이름·소속 자동, 수동 필요→영상 찾기, 중복 의심 배지). Claude Code **/clear 완료** — 다음 킥부터 새 컨텍스트.
- **대표 스모크에서 발견 3건 → 같은 날 결정 완료:**
  ① 이름 다국어 갭(EN 화면에서도 한글) = **새 킥 NAME-I18N-1, W2~3 배치**(nameI18n{ko,en,es}+발행 시 Haiku 1콜, 정본 한글 유지 → #82 불변). W4 베타 게이트에 "EN 화면 로마자 이름" 추가.
  ② 소속 기준 표기 = **영문 공식명(aespa)**, 공식 로마자 없으면 원문 그대로, 솔로는 소속 빈칸 → **W1 소PR**(추출 프롬프트 수정).
  ③ 검수 창이 채운 뒤에도 열려 있어 "빈칸보다 링크가 N개 많아요"가 성공 뒤에 뜸(혼란) → 같은 W1 소PR에서 성공 시 창 닫고 토스트("N칸 채움 · 이름 n · 수동 필요 m" — 문구는 대표 승인 게이트).
- **크라운 카드 이미지 정책 확정(대표 승인):** 화면 = 지금 그대로(그리드·랭킹 썸네일, 매치 임베드). **파일로 굽는 카드 = "허락받은 사진 아니면 무사진 브랜드 카드" 하이브리드.** 무응답=미확보(묵시적 허락 부정 — 법률 상담 확정), 비상업 논리는 방어 아님. 승인 이미지는 인물 단위 자산(이름 키)으로 저장 → MVP2 참가자 DB 첫 열로 흡수. 입력 폼은 저장 1칸+입구 2개(파일 업로드/URL은 우리 스토리지로 복사, 핫링크 금지)+증빙 메모. 폼 착수는 첫 승인 이미지 확보 시점(W5~6 또는 MVP2 초입 — 미정).
  - 마케팅 서신 발송됨: `marketing/00_strategy/서신_마케팅클로드_크라운카드-이미지정책_2026-08-28.md` (대표가 복사-붙여넣기). 회신 오면 코드 실측 근거 붙여 대응.
  - 티오 후속 약속: **소속사 문의서 표준 문안(한/영)** 작성 — 사용 목적(크라운 카드 명시)·범위·기간·크레딧·회신처 포함. 아직 미작성.

## 다음 세션이 바로 시작할 일: W1 소킥 열차 (9/3까지)
순서: **① OG 연결**(아래 프롬프트 준비됨) → **② 소속표기+검수창 소PR**(위 ②③ 묶음) → **③ GA 연결**(운영: Firebase 콘솔 통합에서 GA 활성화 → G- ID → Vercel 환경변수 NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID → 재배포, 티오가 단계별 안내) → **④ UTM 자동 부착**(lib/crown/shareIntents.ts) → **⑤ 계측 소킥 A**(EVENT_SPEC.md 참조) → **⑥ TOG-1**(뉴스데스크 콘솔 다국어 이관).
- 각 소킥 = 킥 프로세스(킥 발행→실행→검증→PR→대표 머지). 화면 문구는 대표 승인 게이트. 터미널 명령은 한 개씩 별도 코드 상자.

### ① OG 연결 — Claude Code에 붙여넣을 프롬프트 (대표에게 전달)
```
소PR: OG 이미지 연결. 실측 사실: public/og.jpg(98KB, 1200×630, 08-26 교체본)가 아직 git에 올라가지 않은 상태(untracked)다 — 이 파일을 이번 PR에 반드시 포함할 것. app/layout.tsx와 app/launch/layout.tsx의 metadata.openGraph에는 현재 images가 없다. 두 파일의 openGraph에 images: [{ url: "/og.jpg", width: 1200, height: 630 }]를 추가하고, twitter: { card: "summary_large_image" }도 함께 추가해라. 문구(title·description)는 한 글자도 바꾸지 마라(화면·메타 문구 변경은 대표 승인 게이트). 완료 기준: ① 빌드 통과 ② 로컬에서 두 경로(/, /launch)의 <head>에 og:image·twitter:card 메타 태그가 렌더되는 것을 실측해 보고 ③ PR 생성. 머지·배포 후 검증 방법(카카오톡/X 카드 미리보기 캐시 갱신 포함)을 표로 보고해라.
```
- 머지·배포 후 대표 검증: https://www.opengraph.xyz 또는 카카오톡 나에게 보내기로 worldcrown48.com 링크 미리보기 확인(카카오는 캐시 때문에 https://developers.kakao.com/tool/debugger/sharing 에서 초기화 필요 — 티오가 그때 단계별 안내).

## 운영 알림 (날짜 순 — 전부 유효)
- **9/1 전후: GCP 결제 활성화 상기** (무료 체험 만료 ≈9/10. 티오가 먼저 꺼낼 것)
- **W2: 발신 메일 구축 착수** (W3 사전등록 개시 전 가동)
- **W2~3: sitemap+Search Console 소킥 · 공유 도착지 수정 · NAME-I18N-1 킥**
- **W3: YouTube 쿼터 증설 신청**
- **W4: 베타 스모크 게이트** (+ EN 화면 로마자 이름 항목 신설)
- **W5: Node 20 → 새 런타임 소킥** (10/30 폐기)

## 세션 운영 메모
- 터미널 명령은 한 개씩 별도 코드 상자 + 성공 신호 명시
- Claude Code 입력창의 문장 = 예상답안. 대표가 실제 행동 후 보내도록 환기
- 산출물은 SendUserFile + 프로젝트 폴더 저장(마케팅 문서는 marketing/00_strategy/)
- 컴퓨터 연결(브리지)이 오늘 자주 끊겼다 — 끊기면 한 번만 재시도, 안 되면 클라우드에서 만들고 나중에 폴더 저장
