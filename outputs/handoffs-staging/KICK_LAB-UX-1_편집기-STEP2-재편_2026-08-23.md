# KICK LAB-UX-1 — 편집기 STEP 2 재편 (결과물 2버튼 + 1:1 썸네일 그리드 + 플래그 배지 + 폼 정리)
발행 2026-08-23 **v1.0 · 대표 승인** · 담당 Claude Code · 예상 5~7h (PR 2개 분할) · 선행: AI-2 ✅(PR #74 5커밋 + #75, main 132c96e) · main 최신(git pull)
> 근거 목업(대표 승인): `outputs/LAB-UX-1_STEP2_목업_v2_2026-08-23.html` · 진단: `outputs/언어토글_진단_2026-08-23.html`

---

## §0 목적 / 완료 정의(DoD)

**목적**: The Lab STEP 2를 "부품(AI 채우기·소싱·검수)을 하나씩 조작하는 화면"에서 **"결과물(동영상/이미지)을 주문하고, 손볼 칸만 고치는 화면"**으로 재편한다. 동시에 (a) 지금 로그·서버 판정에만 있는 **중복 의심·이름↔힌트 불일치**를 슬롯 배지로 노출하고(AI-2 후속 ②), (b) 슬롯 폼을 이름+소속+국가로 정리하며(포지션·이미지 URL 칸 제거), (c) 편집기 안 미번역 잔재(게이트 2화면·자유 텍스트 국적)를 정리한다. 결과는 전부 "제안"이고 발행은 사람이 [토너먼트 생성]을 눌러야 일어난다(R6 불변).

**DoD** — 전부 ✅여야 종료
- [ ] **2버튼 생성 패널**: [🎬 동영상 생성] = 채우기→소싱→검수 자동 체인(시작 전 쿼터 견적 다이얼로그 1회 유지 — 대표 확정) / [🖼 이미지 생성] = 채우기→검수. 부품 버튼 4개(AI 48명 전체·빈칸만 AI·유튜브 검수기·영상 자동 소싱) 상시 노출 제거.
- [ ] **"직접 손보기 도구" 접이식 줄**(기본 접힘): [링크 직접 붙여넣기](구 유튜브 검수기 일괄 입력) · [빈칸만 AI로 채우기]. (대표 확정 B안)
- [ ] 자동 체인 중간 실패 시 **앞선 배치 결과는 그리드에 남고**(기존 부분 결과 계약 유지) 실패 지점·남은 칸 수를 토스트로 보고.
- [ ] **그리드 8열 × 6행**, 슬롯 미리보기 = **유튜브 썸네일 1:1 중앙 크롭**(CSS cover). 빈 칸 금색 점선 유지.
- [ ] **슬롯 폼 = 이름 + 소속(팀) + 국가. 포지션 칸 삭제.** 실측 주의: 현재 AI가 position 칸에 소속(BLACKPINK 등)을 넣는 사례가 프로덕션에서 확인됨(2026-08-23 스크린샷) — 필드 계약을 실사용에 맞춰 공식화하는 작업이다. `aiFillContestants` 프롬프트·스키마의 position → 소속 전환 포함(스키마 변경이므로 파서·클라이언트 동시 수정, AI-2의 R1과 달리 이번 킥은 스키마 변경이 IN).
- [ ] **국가 칸 표준화**: 자유 텍스트("대한민국") 저장 금지 → 표준 코드(ISO 3166-1 alpha-2, 예: KR) 저장 + 표시 시 언어 설정(KO/EN/ES)에 맞는 국가명 렌더. AI 채우기도 코드로 반환하도록 프롬프트 수정. (2026-08-23 스크린샷 실측: lang=es에서도 "대한민국" 노출 — 콘텐츠 미번역 갭의 해결책)
- [ ] **이름 변경 시 자동 해제**: 이름을 다른 문자열로 고치면 이전 인물의 videoId·배지·힌트를 해제(현재는 완전 삭제 시에만). 이름 칸 ✎ 상시 표시.
- [ ] **신설 배지 2종**: ①중복 의심(같은 정규화 키의 칸이 2개 이상 — AI-2의 normalizeNameKey 재사용, 배지에 상대 칸 번호 + 클릭 시 해당 칸으로 스크롤) ②이름↔힌트 불일치(힌트 속 인물 토큰이 이름과 다름 — 서버 판정 로그를 화면 상태로 승격). 기존 배지 3종(제안·수동 필요·실존 의심)과 공존.
- [ ] **검색 힌트 문구 카드 기본 숨김** → 배지·툴팁으로 이동(카드 높이 축소).
- [ ] **이미지 URL 칸 제거**: 먼저 imageUrl을 읽는 모든 곳 grep 실측(발행 문서·매치 화면·Crown Card·랭킹 포함) → 실측 보고 후 UI 칸 삭제 + 신규 저장 중단. 기존 데이터 물리 삭제는 실측 결과에 따라 같은 킥 안에서 하되, 읽는 곳이 남아 있으면 STOP(§6).
- [ ] **프레이밍 가점(결정 4 = A+B, 대표 확정)**: 소싱 후보 선택 시 "단독 인물·얼굴이 크게 나온 썸네일" 가점(감점 아님·제외 아님). AI 판정 정확도·증분 비용을 골든으로 실측 → 표로 보고 후 적용. 실측이 나쁘면 STOP(§6). ※C(얼굴 인식 스마트 크롭)는 OUT — 수정 필요 시 예비 소킥.
- [ ] **키워드 재클릭 결함 수정**: 키워드 개수 상한 로직의 20/12 불일치(재클릭 시 한도가 달라지는 현상) 재현 → 원인 실측 → 단일 상수화 + 단위 테스트.
- [ ] **게이트 2화면 t() 이관**: AdminAuthGuard·DesktopOnly의 하드코딩 한글 → 메시지 카탈로그(ko/en/es). (뉴스데스크 콘솔 이관은 OUT — TOG-1 별도 소킥)
- [ ] **Step2Summary 안내 1줄**: "제목·설명은 발행 시 KO/EN/ES로 자동 번역됩니다" — 편집기에서 원문만 보이는 것이 정상임을 명시(2026-08-23 대표 혼동의 재발 방지).
- [ ] 버튼명 전부 "하는 일" 기준(목업 v2 ⑤ 개명표 준수). 새 문구는 전부 카탈로그 키(ko+en 필수, es 포함)로 추가 — 하드코딩 0건.
- [ ] `TOTAL_CONTESTANTS` 단일 소스 유지(48 리터럴 0건 grep) · hex guard · tsc · build · 단위 테스트 green · PR 4블록 · 프리뷰 SHA=HEAD.
- [ ] 대표 스모크(프로덕션): [🎬 동영상 생성] 1회 완주 → 배지·8열 그리드·1:1 썸네일·이름 수정 해제·중복 배지 눈검사 → 결과 표 기록.
- [ ] 최종 보고 "핸드오프와 달랐던 점" + 실측 표.

## §1 필독 컨텍스트 (인라인 — 외부 메모리 참조 없음)

**확정 결정(대표, 2026-08-21~23)**
① STEP 2 = 결과물 2버튼(동영상/이미지). 부품 버튼 숨김 = **B안**(접이식 "직접 손보기 도구"). 결과 전부 "제안" + 사람이 [토너먼트 생성](R6).
② 표시: 매치 화면 = 정지 임베드(임베드 파사드 법리) — **이번 킥에서 무수정**. 그리드 = 유튜브 썸네일 **1:1 정사각**(16:9 기각 — 숏폼이 작게 나옴). 썸네일 법무는 다음 법률 상담 확인 목록(진행 비차단).
③ 슬롯 폼 = 이름+소속+국가, 포지션 삭제. 8열 그리드.
④ 쿼터 확인 다이얼로그 유지(search.list 하루 100콜, 48명 소싱이 절반 소비).
⑤ 바스트샷 = A(1:1 크롭)+B(프레이밍 가점) 이번 킥. C(얼굴 인식 크롭)는 예비.
⑥ 이름 표기 = "지수 (JISOO)" 한/영 병기 단일 문자열 현행 유지, 무번역, ES는 영문 독해(NAME-1 대형 킥 취소, 2026-08-23).

**증거(2026-08-23 대표 스크린샷 3장, /admin/lab?lang=ko·en·es)**: UI 문구 층은 토글 정상(Create Tournament/Crear Tournament, AI all 48/IA los 48, Image URL/URL de imagen 전부 전환). 미전환 = 콘텐츠 층 — (a) 제목·설명: 발행 시 번역 설계라 편집 중엔 원문만(정상 동작, 안내 문구 부재가 문제) (b) 이름: 정책상 무번역(정상) (c) **국적 "대한민국": 자유 텍스트 데이터 — 실제 갭, 이번 킥의 국가 코드 표준화로 해결**.

**현재 코드(이번 세션 실측)**: `components/admin/lab/TournamentCreator.tsx`(오케스트레이션·fillWithAI·refreshSlot·publish), `FillToolbar.tsx`(AI 2버튼 — 제거 대상), `AutoSourceBar.tsx`(소싱 버튼+쿼터 다이얼로그 — 다이얼로그 로직은 체인 시작부로 이동), `YouTubeInspectorModal.tsx`(검수기 — "링크 직접 붙여넣기"로 개명·접이식 이동), `ContestantGrid.tsx`(6열 고정 — 8열로), `ContestantEditor.tsx`(1:1 imageUrl 미리보기+4필드 — 재구성), `Step2Summary.tsx`, `SlotVideoTuner.tsx`(유지), `AdminAuthGuard.tsx`·`DesktopOnly.tsx`(하드코딩 한글), `lib/lab/`(sourcingDraft·videoDraft·publishReady·keywordsValidation 등 순수 층), `functions/src/core/aiFillCore.ts`(프롬프트)·`parseContestants.ts`(normalizeNameKey — AI-2)·`_embed/sourcing/relevance.ts`(감점 로직 — AI-2 B2), `lib/i18n/messages.ts`(카탈로그, ko+en 필수·es 폴백). 배지 상태는 `SourcingStates`(화면 전용, 발행 안 됨) — 신설 배지 2종도 같은 원칙.

**용어**: 체인 = 버튼 한 번에 채우기→소싱→검수를 순서대로 실행(각 단계는 기존 콜러블 재사용, 새 서버 API 금지가 기본). 프레이밍 가점 = 후보 영상 중 썸네일이 단독 인물·얼굴 위주인 것에 점수를 더 주는 것(제외 아님).

## §2 규모 실측
클라이언트 컴포넌트 약 8파일 수정 + 신규 1~2파일(생성 패널·접이식 도구) + lib/lab 순수 층 2~3파일 + functions 2파일(프롬프트·relevance) + messages.ts 키 추가 + 테스트. **실측 후 규모가 다르면 보고하되 계속.** 이미지 URL 데이터 삭제 범위와 position→소속 마이그레이션(기존 발행 Tournament의 position 값 처리)은 grep·DB 실측 후 A-STOP 보고로 확정.

## §3 스코프
**IN**: §0 DoD 전 항목. 스키마 변경(position→소속, nationality→코드) 포함 — 파서·클라이언트·발행 문서 동시 수정 + 기존 데이터 마이그레이션 방침 실측 보고.
**OUT**: 뉴스데스크 콘솔 문구 t() 이관(**TOG-1 별도 소킥** — 이 킥 종료 후 즉시), 얼굴 인식 스마트 크롭(C — 예비 소킥), 매치 화면·Arena 변경 일체, 참가자 DB 킥(실존·소속·논란 검증 층), NAME-1(취소됨), Haiku 채우기 실험, 브래킷 크기 가변화, 모델 변경.

## §4 RULE
- R1 R6 불변: 어떤 자동 체인도 Tournament를 직접 만들지 않는다. 발행은 [토너먼트 생성] 클릭뿐.
- R2 새 서버 콜러블 신설 금지가 기본 — 체인은 기존 aiFillContestants·autoSourceVideos·검수 경로를 클라이언트에서 순차 호출. 불가피하면 STOP.
- R3 쿼터: 체인 시작 전 견적 다이얼로그 1회(기존 드라이런 재사용). 스모크 search ≤ 50콜, 잔여 확인 후 실행(리셋 KST 16:00).
- R4 새 UI 문구는 전부 메시지 카탈로그 키(ko+en 필수, es 동시 작성). 하드코딩 한글/영문 0건 — grep으로 증명.
- R5 48 리터럴 금지(`TOTAL_CONTESTANTS` 파생) · 디자인 계약 준수(Crown Gold 단일 포인트, 라디우스 0/5/999, 라운드명·LIVE·Vote Rate 미노출) · hex guard.
- R6 이미지 URL 데이터 삭제는 "읽는 곳 0건" grep 증명 후에만. 증명 실패 시 UI 숨김+저장 중단까지만 하고 STOP.
- R7 골든·스모크는 대표 실행(시크릿·어드민). 브라우저 스모크는 대표가 Chrome 어드민 로그인 시 Claude Code 대행 가능(LAB-EV-2 방식). 터미널 명령은 한 줄씩.
- R8 프롬프트 문구 변경 후 골든 3회 + 눈검사 필수(AI-1·AI-2 전례: 문구가 이웃 범주로 번짐).

## §5 Phase (PR 2개 분할 제안)
**PR-1 (화면 재편)**
- **A** 순수 층: 8열 그리드 상수·1:1 썸네일 크롭·이름 변경 시 해제 규칙(`videoDraft`/`sourcingDraft` 확장)·중복 의심 파생 함수(normalizeNameKey 재사용)·이름↔힌트 불일치 판정 함수 — 전부 pure + 단위 테스트.
- **B** 생성 패널·접이식 도구·개명: FillToolbar/AutoSourceBar 재구성 → 2버튼+체인 오케스트레이션(TournamentCreator), 검수기 개명·이동, 카운터("채움 n/48 · 제안 n · 손볼 칸 n").
- **C** 슬롯 카드 재구성: 폼 3필드(이름·소속·국가)·✎·배지 5종·힌트 툴팁·[상대 칸 보기] 스크롤.
- **D** 게이트 2화면 t() 이관 + Step2Summary 번역 안내 문구 + 키워드 20/12 결함 수정.
**PR-2 (데이터·소싱)**
- **E** 스키마 전환: position→소속, nationality→ISO 코드(표시 렌더러 3언어) + aiFillCore 프롬프트·parseContestants 수정 + 골든 3회(48명·코드 유효성·소속 정확도 눈검사).
- **F** 이미지 URL: 읽는 곳 grep 실측 보고 → 칸 삭제·저장 중단·데이터 정리(§4 R6 게이트).
- **G** 프레이밍 가점: relevance.ts 확장 + 정확도·비용 골든 실측 표 → 적용/보류 대표 결정.
- **H** 배포(functions 포함, 대표 터미널 한 줄씩) → 대표 스모크(§0 마지막 항목) → 최종 보고.

## §6 Auto-STOP
- 체인을 기존 콜러블 재사용으로 못 만들고 새 서버 API가 필요할 때.
- position→소속·국가 코드 전환이 기존 발행 Tournament 표시를 깨뜨릴 때(마이그레이션 방침 선택지와 함께).
- imageUrl 읽는 곳 grep이 0건이 아닐 때(남은 곳 목록과 함께 — 데이터 삭제 보류).
- 프레이밍 가점 골든에서 정확도가 낮거나(오판이 정상 무대 영상을 밀어냄) 비용 증가가 유의미할 때(수치와 함께 — A만 적용하고 B 보류 선택지).
- 중복 의심 판정이 동명이인(다른 그룹의 같은 이름)을 오탐할 때(AND 조건 강화안과 함께).
- 키워드 20/12의 원인이 상수 불일치가 아니라 설계 충돌일 때.
- 골든에서 48명 미달·스키마 파싱 실패 반복 시(R8).

## §7 검증 하네스
단위 테스트(순수 층 신규 규칙 전부) · 골든 3회(스키마 전환+프레이밍) · hex guard · 48/하드코딩 문구 grep 0 · 프리뷰 렌더(8열·1:1·배지 5종 스냅샷) · 프로덕션 스모크 1회(체인 완주 표) · lang=ko/en/es 3회 화면 확인(편집기 잔여 하드코딩 0 — 대표 스크린샷 방식 재현).

## §8 PR 본문 4블록
A 변경 요약 · B 실측(imageUrl grep 표, position 실데이터 표본, 골든 3회 표, 프레이밍 정확도·비용 표) · C 검증 근거(§7) · D 남긴 것(TOG-1 뉴스데스크 이관, C 스마트 크롭, 참가자 DB 킥, es 미보유 키 잔여) · E 사용자 영향("운영자: 버튼 7개 → 2+접이식, 손볼 칸이 배지로 보임, 화면 스크롤 25% 단축").

## §9 대표 사전 준비물
없음(Secret 기존). 스모크 전 Search Queries 잔여 ≥ 50 확인. 골든은 대표 터미널(한 줄씩 안내 예정), 브라우저 스모크는 Chrome 어드민 로그인.

## §10 "핸드오프와 달랐던 점" 보고 의무
킥 문면과 다르게 한 모든 것을 최종 보고에 기록. 스모크 표는 Cowork 메모(lab-ux1-decisions) 갱신에 사용.

## §11 후속 예약 (이 킥 종료 직후)
- **TOG-1(소킥)**: 뉴스데스크 콘솔 문구 t() 이관 — DoD: 토글 ko/en/es에서 콘솔 하드코딩 문구 0건. (대표 확정 2026-08-23: "번역 정당성 시험을 위해 운영 화면도 토글 대상")
- 참가자 DB 킥(별도 확정 준비) · C 스마트 크롭(예비).

*v1.0 · 2026-08-23 · 대표 승인 — 발행*
