# KICK — C-1·G-1 E2E 실기 실패 4건 조사 (조사 전용)

작성: 2026-08-02 | 발신: Cowork(진행 관리) | 수신: Claude Code

---

## 0. 확정 사실 (재확인 불필요)

- `origin/main`에 PR #47(ND-1 본체)·#48(폴리시 5건)·#49(CI head-SHA 일괄 전환)·#50(조사 메모) **전부 머지 완료**. 로컬 main은 대표가 `git pull origin main` 완료한 상태에서 시작한다 (로컬 HEAD가 #48 `e0c5d45`에 머물러 있으면 **중단하고 대표에게 pull 요청**).
- PR #49로 C1·C2·D1·G1 워크플로우의 프리뷰 해석은 **head-SHA 기준으로 이미 올바름**. D-1·C-2 = 완전 green.
- 아래 4건은 **프리뷰 함정이 아니라 실기 실패**다. `Resolve Preview URL`·`Wait for reachability` 스텝은 전부 통과(✓)했고 실패는 E2E 본체에서 발생했다.
- 증거 run id: C-1 `30546736137` · G-1 `30546736135` (`gh run view <id> --log-failed`).
- 미사용 시크릿 `C1/C2/D1/G1_PREVIEW_URL` 4종은 이미 삭제됨 — 없다고 이상한 것 아님.

## 1. 필독 (시작 전)

1. `outputs/handoffs-staging/ND1-followup-e2e-body-failures-2026-07-30.md` — **이 조사의 단일 진실.** 실패 4건별 에러·가설·spec:line·볼 곳·재현법 수록.
2. `CLAUDE.md` (프로젝트 규칙) — 단, CLAUDE.md의 AI-Report 표기 등 일부 항목은 낡았음(개정 대기). 표기·디자인 판단은 하지 말 것 — 이번 태스크 스코프 밖.

## 2. 스코프 — 조사 전용 (수정 금지)

**하는 것:** 4건 각각의 원인 판정 + 증거 수집 + 권고안 보고.
**하지 않는 것:** 앱 코드·스펙 수정, PR 생성. 판정 보고 → 대표 승인 후 별도 태스크로 수정한다.

## 3. 조사 절차 (메모의 공통 다음 단계 준수)

1. **트레이스 먼저**: `gh run download 30546736137` / `30546736135` (또는 로컬 재현의 `test-results/**/trace.zip`) → 실제 렌더 DOM·스크린샷 확인 후 판정.
2. **시드 의존 확인**: C-1 #3(vote-left "P1")·#4(mobile320)는 프리뷰에 필요한 시드(5/5 Voter, 활성 Tournament)가 있었는지부터 확인.
3. **global-setup 검증**: G-1 #1(needs-signin)은 `e2e/global-setup.ts`의 storageState 인증 간섭 가설을 먼저 검증 (비로그인 컨텍스트 필요 여부).
4. **우선순위**: **② G-1:124 Domain 6 live link 최우선** — /news 접근성은 인터넷신문 등록 심사와 연관 가능성. 이후 ① → ③ → ④.

## 4. 완료 기준 (Definition of Done)

4건 **각각**에 대해 아래 3분류 중 하나로 판정 + 증거 첨부:

- **(a) 앱 회귀** — 어느 커밋/변경이 유발했는지 특정 (또는 후보 범위)
- **(b) 스펙 노후** — 앱의 현재 동작이 맞고 스펙이 낡음 (근거: 어떤 결정/커밋으로 낡았는지)
- **(c) 전제 문제** — 시드/CI 인프라 전제 결함 (테스트 자체는 유효하나 환경 미비)

각 판정에: 증거(트레이스 스크린샷·DOM 발췌·run 로그 라인) + 권고 수정안(수정 위치·방향, 예상 규모) + 모듈별(C-1/G-1) PR 분리안.

보고 언어 규칙: 빨간불을 "가짜"·"무시 가능"으로 표현 금지. 항상 위 3분류로 말한다.

## 5. 금지 사항

- **frozen-preview 재도입 금지** — 고정 `*_PREVIEW_URL` 시크릿 재생성 금지. 프리뷰 해석은 head-SHA로 이미 올바름.
- 테스트 삭제·`skip` 처리로 red를 지우는 것 금지 (판정 (b)여도 스펙 수정은 다음 태스크에서).
- 이번 태스크에서 앱 코드 수정·머지 금지.
- Round Scope Lock·Vote Rate 노출 금지 등 제품 규칙 임의 변경 금지.

## 6. Superpowers / TDD (다음 단계 예고)

이번은 조사 전용이라 코드 수정이 없다. 단, 후속 수정 태스크에서는 **이미 존재하는 실패 테스트가 곧 RED** — 수정 후 해당 spec이 GREEN이 되는 것을 완료 조건으로 삼는다 (스펙 노후 판정 건은 스펙 수정 → GREEN). 후속 핸드오프에 §11 Superpowers 지시가 포함될 예정.

---

*© 2026 WorldCrown48 | Kick Prompt | outputs/handoffs-staging/*
