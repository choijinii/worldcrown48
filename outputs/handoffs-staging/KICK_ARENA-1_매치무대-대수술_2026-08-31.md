# KICK ARENA-1 — 매치 무대 대수술 (VS 스플릿 · 라운드 전환 · 결승 · 대관 연출 · 첫 입장 팝업)

발행 2026-08-31 (v0.9 초안 — 대표 검토 대기) · 승인 대표 · 담당 Claude Code(구현) + Claude Design(시각 층·프로토타입) + Cowork 티오(킥·게이트·검증) · 예상 10영업일(W2~3, 9/4~9/17) · 선행 조건: PR #89 머지 완료(✅ 08-31) · Claude Design 핸드오프 번들 수령

> v0.9 — 08-31 밤 초안. 대표 검토 + Claude Design 산출물(§C) 수령 후 v1.0 발행.

| 항목 | 값 |
|---|---|
| 킥 코드 | `ARENA-1` |
| 브랜치 | `feat/arena-1-match-stage` |
| 워크트리 | `~/Projects/worldcrown48-arena-1` |
| Base 브랜치 | `main` |
| 기준 HEAD | `85a56db` (2026-08-31, PR #89 머지 직후) |
| 후속 킥 | `ARENA-2` 아레나 홈 + 브래킷 크기(12/24/48) 선택 UI — 별도 문서 |

---

## §0 목적 / 완료 정의(DoD)

**목적**: 첫 화면의 열쇠인 아레나 매치 무대를 "카드 2장 그리드"에서 **화면 전체를 좌우로 가르는 VS 스플릿 무대**로 바꾸고, 결승 픽 직후 **대관 연출(Crown Ceremony)** 을 넣어 크라운 카드 공유가 시작되는 감정의 정점을 만든다. 2026-10-08 런칭의 핵심 체험이며, 총공 3일의 공유 전환율을 결정한다.

**DoD**
- [ ] `/arena/{id}` 매치 화면이 100vw×100vh 좌우 50:50 스플릿으로 렌더되고, 호버(데스크톱)/1탭(모바일)한 쪽이 **flex 1.5~2배로 벌어지며 scale 1.1 + 채도 30→100%**, 그쪽 영상만 10초 무음 루프 재생(영상 없으면 썸네일 정지), 반대쪽은 정지·감채도.
- [ ] 매치 화면 어디에도 라운드 라벨·득표율·마감 타이머·개발자 고지문(`vs-foot`)이 **없다** (E2E 어서션으로 판정).
- [ ] 결승(3인 1택) 픽 직후 대관 연출이 2.5~3초 재생되고 **정지 프레임에서 탭을 기다린다**. 도중 탭 → 즉시 정지 프레임. `prefers-reduced-motion` → 정지 프레임만. 탭 후에만 크라운 카드.
- [ ] 라운드 전환 화면에 라운드명이 나오고(유일한 노출처), "Voter" 문자열이 팬 노출 문구에 0건.
- [ ] 첫 입장 팝업이 기기당 1회만 뜨고(localStorage), 닫기 전엔 투표 불가.
- [ ] 계측: `tournament_start` · `round_advance`(라운드별 + final) · `champion_confirmed` 가 대수술 후에도 **동일 파라미터로 발화**(GA4 DebugView 실측 캡처 첨부) + 신규 `ceremony_viewed` · `ceremony_skipped`.
- [ ] 팬 노출 문구 전부 `arena.*` i18n 키(ko/en/es) — 하드코딩 문자열 0건(grep 판정).
- [ ] 유닛 vitest · 통합(Emulator) · E2E Playwright(`c1-arena-flow`·`c2-crown-card-flow`·`hf3-guest-run` 회귀 + 신규 `arena1-split-stage`·`arena1-ceremony`) 100% PASS, Console 에러 0건, `npm run check:hex` 위반 0건.
- [ ] 라이트하우스 모바일 Performance ≥ 80 (매치 화면, Moto G Power 프로파일) — 영상 iframe 최대 1개 동시.
- [ ] 최종 보고에 "핸드오프와 달랐던 점".

## §1 필독 컨텍스트 (인라인)

**리포 내 필독**: `CLAUDE.md`(대진 흐름 #1~#10, 불변 원칙, Stale-Doc Guard 절) · `LANGUAGE.md` §표시 용어 · `DESIGN_BRIEF.md` · `docs/design/wireframes/Domain 3 · The Arena.html` · `docs/lite-specs/C1-vote-engine.md`(⚠ VoteRateBar 절은 폐기 — 아래) · `components/embed/LoopPlayer.tsx` · `lib/embed/constants.ts` · `marketing/00_strategy/대관연출_인계_Crown_Ceremony_2026-08-31.md`.

**확정 결정 (날짜순)**
- 2026-07-10: 라운드 흐름 48→24→12→6→THE FINAL(3인 1택, 1v1로 쪼개지 않음). 완주 = 46표. Round Scope Lock(라운드명은 전환 화면에서만). Ranking Scope Lock(득표율·Crown Score는 랭킹 도메인만). "Arena 홈 = 활동" 신설과 브래킷 크기 선택 UI는 **ARENA-2**.
- 2026-08-09: 테마 B안 하이브리드 — 매치·투표는 **다크**. 첫 화면 열쇠 = Arena. VS 스플릿 100vw/vh Flexbox 50:50, 호버 flex 1.5~2 + scale(1.1) + saturate(30%→100%), VS 뱃지 중앙, 터치(상하 2탭), 픽 절정 연출, 다음 매치 프리로드, vs-foot 고지문 무대 제거(규칙은 유지), Pitch 카드 축소판(ARENA-2/Pitch 킥으로 이월), 임베드 10초 루프(LoopPlayer 재사용), MAX_CONCURRENT_PLAYERS=6.
- 2026-08-22: 표시 = 매치 화면 임베드 파사드 법리 / 그리드 = 유튜브 썸네일.
- **2026-08-31 (대표)**: ① ARENA-1/2 분할. ② **영상은 자동재생하지 않는다 — 호버/탭한 쪽만 10초 무음 루프 재생**(기본 정지 포스터). 08-09 "10초 루프"와 08-22 "정지 임베드"는 이렇게 통합. ③ 실행 = Claude Design(시각 층·프로토타입·번들) → Claude Code(구현·TDD). ④ 마케팅 인계 "대관 연출"을 이 킥에 포함(단독 과제 아님).
- 2026-08-31 부하 실측: 1인당 투표 속도 제한 20/분(`functions/src/onVote.ts RATE_LIMIT`)이 열성 팬을 막을 수 있음 → 이 킥에서 **40/분**으로 완화(대표 승인 시).

**용어(기존 정의 그대로)**: Tournament · Contestant · Match · Voter(코드·DB) / 팬·Fan(팬 노출 문구) · Champion · Crown Card · Tournament Deadline(Round Deadline 없음) · THE FINAL. FIFA 명칭(ROUND OF 16/QUARTERFINAL/SEMIFINAL) 금지. 전투 은유 금지(매치 = "어느 쪽을 더 사랑하는지 고르는 순간").

**표시 용어 v2.0 §1 (인라인 — 이 킥에서 함께 처리)**
1. 팬 노출 텍스트의 "Voter" → ko "팬" / en·es "Fan(s)" (`lib/displayTerms.ts` 재사용). 코드 식별자·DB·어드민은 Voter 유지.
2. 참가자 이름 표기: 언어 설정(KO/EN/ES)에 따름 — 현재 데이터는 "한글 (영문)" 병기 원문이 있으므로 ARENA-1은 **원문 그대로 표시**하고 규칙 확정은 NAME-I18N-1(W2~3 병행 소킥)에 위임. 이 킥에서 이름 문자열을 가공하지 않는다.
3. 편집기 키워드 20/12 결함은 이 킥 범위 아님(LAB 후속).

**Stale-Doc Guard (인라인)**: 이 킥으로 바뀌는 규범 문서 — `CLAUDE.md`(대진 흐름 #5 "매치 화면 고지문" 표현 정정, 대관 연출 신설 명시), `docs/lite-specs/C1-vote-engine.md`(§THE FINAL 다음에 "대관 연출" 절 추가, **VoteRateBar 절 폐기 표기**, 라운드 전환 카피 현행화), `docs/design/wireframes/Domain 3 · The Arena.html` 상단에 "ARENA-1 이후 구조는 코드가 진실" 배너, `LANGUAGE.md`에 "Crown Ceremony(대관 연출)" 용어 등재. **머지 PR에 이 문서 수정이 함께 들어가야 DoD 충족.** `docs/design/WC48_DESIGN_SYSTEM_v2.4.md`의 GSAP 절은 이 킥이 건드리지 않되 보고서에 스테일로 표기.

## §2 규모 실측치 (2026-08-31, HEAD 85a56db)

- 교체 대상: `components/arena/{MatchView,ContestantCard,VsSymbol,FinalPickView,RoundTransition}.tsx`(5파일, 합 347줄) + `components/arena/arena.module.css`(605줄) + `app/arena/[tournamentId]/page.tsx`(346줄, 화면 분기·계측 보존) — 실측이 다르면 보고하되 계속.
- 신설: `CeremonyStage.tsx` + `ceremony.module.css` + `useCeremonyTimeline.ts`(WAAPI) · `ArenaIntroModal.tsx` · `useNextMatchPreload.ts` · `SplitStage.tsx` · `StageSide.tsx`.
- 재사용: `components/embed/LoopPlayer.tsx`(179줄, 무음·1:1 크롭·pointer-events:none·에러 시 썸네일 폴백), `lib/media/mediaSlot.ts`(`contestantThumbnail`, `buildEmbedFacade`), `lib/arena/*` 순수 모듈(변경 없음).
- i18n: `lib/i18n/messages.ts` `arena.*` 현 8개 → 약 30개(전환·결승·대관·팝업·대기 문구).
- 계측: `lib/analytics/funnelEvents.ts` 이벤트 3종 보존 + 2종 추가.
- 서버: `functions/src/onVote.ts` RATE_LIMIT 20→40 (1줄) + 테스트 상수.
- 자산(대표 제작, §9): `public/ceremony/{crown_main.svg, crown_glow_back.png, crown_glow_front.png(선택), sparkle_01~03.png}`.

## §3 스코프 IN / OUT

| | 내용 |
|---|---|
| **IN** | M1 VS 스플릿 무대(호버/탭 재생) · M2 픽 절정 연출 + 다음 매치 프리로드 · M3 라운드 전환 재설계(동심원·코너 라벨 복원, 표시 용어, reduced-motion 대기 단축) · M4 결승 3분할 + ★대관 연출 · M5 첫 입장 팝업 · M6 문구 전부 i18n + vs-foot 제거 + "카드 준비 중…" 대기 문구 · M7 계측 보존 + ceremony 2종 · M8 RATE_LIMIT 40 · 규범 문서 4건 갱신 |
| **OUT** | 아레나 홈·브래킷 크기 선택(ARENA-2) · 크라운 카드 디자인 변경(별도 시안 논의) · 랭킹 화면 · Pitch 카드 축소판 · 틱톡 임베드 · 참가자 이름 표기 규칙(NAME-I18N-1) · 서버 PNG 생성기(functions/_crown) 정리 · **리팩터링 유혹 금지** — `lib/arena/*` 순수 모듈과 `voteStore`는 손대지 않는다(투표 엔진 불변) |
| **후속 킥** | ARENA-2(홈+브래킷 크기, W3 후반~W4) · 랭킹 개편 W4 · Pitch 쇼케이스 W5 · 뉴스룸 승격 W5~6 |

## §4 RULE

- **R1 투표 엔진 불변** — `onVote` 콜러블 호출 시그니처·`roundProgress` 구독·`matches/roundProgress/bracketSeed` 순수 모듈·Firestore 스키마를 바꾸지 않는다. 화면만 바꾼다. 어기게 되면 STOP.
- **R2 Round Scope Lock** — 매치·결승·대관 화면에 라운드 라벨·"N강"·진행 HUD·득표율·마감 타이머·Crown Score를 렌더하지 않는다. 라운드명은 `RoundTransition`에서만.
- **R3 동시 플레이어 ≤ 6, 실제 목표 ≤ 1** — 매치 화면에서 재생 중인 iframe은 호버/탭한 쪽 **1개**. 프리로드는 iframe이 아니라 썸네일·포스터·다음 videoId 준비까지만(iframe 사전 생성 금지 — 모바일 메모리).
- **R4 자동재생 금지** — 화면 진입 시 어느 쪽도 재생하지 않는다. 재생은 사용자 의도(호버/탭) 이후에만. 재생은 무음·playsinline·controls=0·클릭차단·출처 칩 "▶ YouTube" 유지(임베드 파사드 법리).
- **R5 모션은 위치·크기·회전·투명도만** — blur·filter 애니메이션 금지(정적 filter는 허용), 레이어 시작 시각 어긋나게, 애니메이션 라이브러리 도입 금지(CSS keyframes + Web Animations API). `prefers-reduced-motion`은 모션 제거 + **대기 시간도 제거**.
- **R6 raw hex 금지** — `var(--token)`만. Crown Gold 단일 포인트, Crimson/Turquoise는 좌/우 상태색으로만. `npm run check:hex` 통과.
- **R7 팬 노출 문구 = i18n 키 + 표시 용어 v2.0** — 하드코딩 금지, "Voter" 금지, 전투 은유 금지. **새 문구는 커밋 전 대표 승인 게이트**(전후 비교표).
- **R8 계측 3종 보존** — 이벤트명·파라미터(`commonEventParams`·`entry_point`·`round`·`champion_id`·`duration_sec`) 변경 금지. 발화 지점이 옮겨가더라도 1회성 ref 가드 유지.
- **R9 대관 연출은 크라운 카드로 자동 전환하지 않는다** — 정지 프레임 + 탭 대기. 정지 프레임은 그 자체로 완성된 화면(사전등록 캡처 소재).
- **R10 CROWNED 문구는 폰트** — 이미지 금지(다국어).

## §5 Phase 분할 (Phase = 커밋 1개, 각 Phase TDD RED→GREEN→REFACTOR)

- **A 무대 골격**: `SplitStage`(100vw/vh flex, 좌우 `StageSide`) + 상태 머신(`idle | focusL | focusR | pickedL | pickedR | loading`) 순수 모듈 `lib/arena/stageState.ts`(유닛 테스트) → `MatchView` 교체. 문구 0개(다음 Phase). 모바일 = 상하 2탭.
- **B 재생·프리로드**: `StageSide`에 LoopPlayer 조건 마운트(focus 쪽만, R3/R4), 반대쪽 포스터. `useNextMatchPreload`(다음 매치 썸네일 `<link rel=preload>` + videoId 준비). 유닛: focus 전환 시 마운트 수 ≤ 1.
- **C 픽 연출 + 오버레이 축소**: 픽 시 선택 쪽 확장 절정(320ms) → 서버 응답 대기 중 스피너는 선택 쪽 안에서만. 실패 시 원위치 + 토스트(기존 오류 키).
- **D 라운드 전환 재설계**: 와이어프레임 `.rt-rings`·코너 라벨·`.rt-meta` 복원, 문구 i18n, 표시 용어, reduced-motion 시 hold 2000→0(즉시 onDone 가능한 "계속" 탭). 유닛: 타임라인 순수 함수.
- **E 결승 3분할**: `SplitStage` 3분할 변형(좌·중·우), 크라운 글리프 복원(`.fcard-crown`), 픽 → Phase F로.
- **F ★대관 연출**: `CeremonyStage` 7층 레이어 + `useCeremonyTimeline`(WAAPI, 키프레임 표는 §C 콘티 기준) + 탭 건너뛰기(`finish()`) + reduced-motion 정지 프레임 + `ceremony_viewed/skipped` 계측 + 탭 후 `CrownCardModal`. 자산 §9. E2E `arena1-ceremony`.
- **G 첫 입장 팝업**: `ArenaIntroModal`(focus-trap 재사용, localStorage `wc48:arena:intro:v1`), 문구 승인 게이트, 개인정보 고지 정합(정책 허브 문구와 대조).
- **H 문구·규범 문서**: 잔여 하드코딩 제거(grep 0건), `vs-foot` 삭제, "카드 준비 중…" 키화, CLAUDE.md·C1-vote-engine·wireframe 배너·LANGUAGE.md 갱신, `onVote.ts` RATE_LIMIT 40.
- **I** PR(4블록) → CI(hex-guard·vitest·E2E) → 프리뷰 눈검증(티오 프리플라이트 → 대표) → 머지 → Vercel 배포 + **functions 배포(별도, RATE_LIMIT 반영)** → 프로덕션 GA4 DebugView 계측 3종 확인.

## §6 Auto-STOP 조건

- 킥 문면과 리포 실물이 다름(파일 없음·구조 다름) → STOP: 근거 경로 + 권장안.
- R1을 지키면 요구를 못 지키는 상황(예: 픽 연출을 위해 `voteStore`에 상태를 넣어야 할 것 같을 때) → STOP: 화면 로컬 상태 대안 제시.
- 모바일에서 "1탭 = 재생·확대, 2탭 = 투표" 규칙이 E2E `hf3-guest-run`(현재 1탭 투표 전제)과 충돌 → STOP: 테스트 갱신 범위 제시.
- Claude Design 번들의 값(간격·크기·타이밍)이 §1 확정 스펙(flex 1.5~2, scale 1.1 등)과 다름 → STOP: 번들 vs 킥 중 어느 것을 따를지(기본: 킥의 수치 규칙 > 번들의 시각 디테일).
- 대관 자산 6종 중 누락·규격 불일치(투명 배경 아님, 여백 잘림, SVG에 고정 hex/필터) → STOP: 임시 플레이스홀더로 진행할지 대기할지.
- 첫 입장 팝업 문구·개인정보 고지 문안이 문서에 없음 → **추측 금지**, STOP(대표 승인 게이트).
- 계측 파라미터를 바꾸지 않고는 구현이 안 될 때 → STOP.
- functions 배포 권한·시크릿 필요 → STOP: 사람이 할 절차.

## §7 검증 하네스

| | 무엇 | 통과 기준 |
|---|---|---|
| 기계 | `npx vitest run` · `npm run check:hex` · `npx tsc --noEmit` | 전부 green, 위반 0건 |
| 기계 | `grep -rn "Voter" components/arena app/arena --include=*.tsx` (팬 노출 문자열) | 0건(코드 식별자 제외) |
| 기계 | `grep -rn "vs-foot\|vsFoot\|Next match\|Choose your Champion" components/arena app/arena` | 0건 |
| 기계 | Playwright: `c1-arena-flow`(10/24 새로고침 이어하기·결승 픽 후 roundProgress만 기록·모바일 캡처) · `c2-crown-card-flow`(결승 픽 → **대관 → 탭** → 모달) · `hf3-guest-run` · 신규 `arena1-split-stage`(스플릿 렌더·라운드/득표율 요소 부재·재생 iframe ≤1) · `arena1-ceremony`(2.5~3.2초 후 정지·탭 건너뛰기·reduced-motion 즉시 정지 프레임) | 100% PASS, Console 에러 0건 |
| 기계 | Lighthouse 모바일(매치 화면) | Performance ≥ 80 |
| 사람 | 프리뷰(Vercel) 데스크톱 1440·모바일 390: 스플릿 호버/탭 확대·재생, 전환 화면, 결승, 대관 연출 정지 프레임 캡처 1장 | 콘티(§C)와 육안 일치 — 디자이너 대표 판정 |
| 사람 | 프로덕션 머지 후 로그인 완주 1회 + GA4 DebugView | tournament_start·round_advance×4+final·champion_confirmed·ceremony_viewed 발화 |

## §8 PR 본문 4블록

`PR_TEMPLATE.md` — A 무엇을 했나 · B 어떻게 확인했나(실측 수치·캡처) · C 판단이 필요했던 것 · D 핸드오프와 달랐던 점 · E 리뷰어 체크리스트(사용자 영향 한 줄: "매치 화면·결승·라운드 전환 전면 교체, 투표 데이터·규칙 무변경").

## §9 준비물 (사람만 할 수 있는 것)

- [ ] **대표**: Claude Design 산출물(§C) — 스플릿 무대 상태 5종, 결승 3분할, 라운드 전환, 대관 연출 콘티 5컷(0.0/0.3/0.8/1.2/정지) + 프로토타입, 첫 입장 팝업 → "Send to Claude Code" 번들.
- [ ] **대표**: 대관 자산 6종(투명 PNG 3배, `crown_main.svg`는 fill=currentColor·필터 없음·정사각 viewBox·패스만) → `public/ceremony/`.
- [ ] **대표 승인 게이트**: 첫 입장 팝업 문구(ko/en/es) · 라운드 전환 문구 · 결승 헤더 · CROWNED 문구 · "카드 준비 중…" · 개인정보 고지 문안.
- [ ] **대표**: functions 배포(RATE_LIMIT) — `firebase deploy --only functions:onVote` 한 줄, 티오가 시점 안내.
- [ ] Firebase Auth 승인 도메인에 프리뷰 URL(로그인 완주 눈검증용) — 없으면 프로덕션 확인으로 대체.

## §10 "핸드오프와 달랐던 점" 보고 의무

최종 보고에 킥 문면과 다르게 한 모든 것(실측 차이·없던 파일·판단 변경·번들과의 차이)을 기록한다.

## §11 정리

- [ ] 워크트리 제거 · 브랜치 삭제 · 메모리 갱신(`arena-kick-2026-08-31`) · `LESSONS.md` · 이 문서 하단 완료 표시 · 마케팅에 "대관 연출 구현 완료 + 정지 프레임 캡처" 회신.

---

## §C Claude Design 설계 요구 목록 (대표 → Claude Design → 번들)

> Claude Design은 저장소 코드를 직접 고치지 않으므로, 여기서 만드는 것은 **시각 층과 타이밍**이다. 아래 수치 규칙은 확정값 — 번들은 그 안에서 디테일(간격·서체 크기·글로우 세기·이징)을 정한다.

| # | 화면 | 상태/컷 | 확정 수치 | Claude Design이 정할 것 |
|---|---|---|---|---|
| C1 | VS 스플릿 무대 (데스크톱 1440×900 / 모바일 390×844) | `idle` · `focusL` · `focusR` · `pickedL` · `loading` | 100vw/vh 50:50 · 포커스 쪽 flex 1.5~2 · scale 1.1 · saturate 30→100% · 가운데 VS 뱃지 · 모바일 상하 2탭 · 라운드/득표율/타이머 없음 · 다크 테마 | 이름·소속 타이포 위치, VS 뱃지 형태, 좌 터콰이즈/우 크림슨 상태색의 세기, 포스터(정지) 상태의 처리, "▶ YouTube" 출처 칩 위치, 이징 |
| C2 | 결승 3분할 | `idle` · `focus중` · `picked` | 3인 동시 · 1택 · 크라운 글리프 | 3분할 비율(중앙 강조 여부), 포커스 시 확장 비율 |
| C3 | 라운드 전환 | 1컷 + 진행바 | 라운드명(ROUND OF 24 등) 유일 노출처 · 동심원·코너 라벨 4개 · 2초 자동 | 동심원·글로우 디테일, 코너 라벨 문구(승인 게이트), "계속" 탭 처리 |
| C4 | ★대관 연출 | 콘티 5컷: 0.0 / 0.3 / 0.8 / 1.2 / 정지(≈2.8) + 프로토타입 | 7층 레이어(딤·탈락2·챔피언·광채 뒤·크라운·반짝임 앞·CROWNED) · 총 2.5~3초 · 크라운 하강은 빠르게→감속→튕김 · 광채 뒤/반짝임 앞 · 정지 프레임 완성도 · 탭 대기 · 모션은 위치·크기·회전·투명도만 | 각 층 시작 시각(어긋남), 크라운 폭(min(vw,vh)의 %), 광채 확대 배율, 반짝임 개수·궤적, CROWNED 서체·등장 방식, 정지 프레임 구도(사전등록 캡처용) |
| C5 | 첫 입장 팝업 | 1컷 | 기기당 1회 · 소개 + 데이터 고지 + "결과는 라운드 후 공개" · 닫기 전 투표 불가 | 레이아웃, 문구 초안(승인 게이트), 닫기 버튼 위치 |

**번들 수령 후 티오 대조**: 수치 규칙 위반 여부(flex·scale·saturate·플레이어 1개·자동재생 없음) → 위반 시 킥이 이김(§6).

---

## §D 대관 연출 기술 메모 (Claude Code용)

- 타임라인: `useCeremonyTimeline(root, { reducedMotion })` — `element.animate(keyframes, { duration, delay, easing, fill: 'forwards' })` 7개를 한 컨트롤러로 묶고, `skip()` = 모든 `Animation.finish()`. 완료 콜백 후 `data-ceremony="hold"` → 탭 리스너 등록 → `CrownCardModal`.
- 레이어 z-order(아래→위): dim(1) · loser×2(2) · champion(3) · glow_back(4) · crown(5) · sparkle×N(6) · CROWNED(7). `glow_front`(선택) = crown 위, sparkle 아래.
- 크라운 하강 이징: `cubic-bezier(.2,.9,.3,1.15)`(오버슈트) 또는 keyframes 3점(−120%→+4%→0). 닿는 순간(≈0.8s)에 glow_back 확대 0.6→1.0 + opacity 0→.9, crown 색 `--crown-gold` 밝기 1.0→1.35→1.0 (currentColor 필터 대신 두 겹 SVG 크로스페이드 — R5).
- 자산 로딩: 결승 화면 진입 시 6종 `<link rel=preload as=image>`(대관 직전 지연 0).
- reduced-motion: 정지 프레임 즉시 렌더, 탭 대기 동일. `ceremony_viewed` 파라미터 `reduced_motion: true`.
- 계측: `ceremony_viewed { tournament_id, champion_id, reduced_motion }` 정지 프레임 도달 시, `ceremony_skipped { elapsed_ms }` 도중 탭 시.
- 실패 안전: 자산 로드 실패 시 크라운 글리프(기존 SVG) 폴백, 연출은 진행.

*v0.9 · 2026-08-31 · 티오*


---

## v0.95 추가 (2026-09-03, 마케팅 왕복 2차 반영)

1. **대관 연출 = 선행 소킥 ARENA-1a로 분리**(마케팅 요청 수용, 대표 전달): 콘티 5컷 + 자산 6종 도착 즉시, **현재 결승 화면(FinalPickView) 위에** CeremonyStage를 먼저 구현·배포. 스플릿 무대(Phase A~E)와 독립 — 이후 대수술 때 그대로 승계. Phase 순서: F(대관, 선행) → A~E → G~I.
2. **CROWNED 문구 확정(마케팅 발신·대표 전달)**: ko "크라운 획득" / en "CROWNED" / es "CORONADO" — 카피 규칙('왕관' 한자어 금지) 정합 확인. i18n 키 `arena.ceremony.crowned`.
3. **match_session_id 신설**: `hash(uid + tournamentId).slice(16)` 결정적 생성(저장 불필요, 기기 간 동일 — 한 사람이 같은 대회 재도전 불가라 판=사람×대회 1:1, 서버 차단 실측). 탑재: ceremony_viewed/skipped · crown_card_created · crown_downloaded · crown_shared_x · crown_shared_native.
4. **first_vote 이벤트 추가**(판당 1회) — 깔때기 3종 중 유일한 공백(진입·완주는 발화 중, round_advance는 24표 후라 부적합). match_session_id와 같은 소킥에 묶음.
5. founding_rank는 이 킥 범위 아님 — 사전등록 소킥(콜러블화)으로. 슬러그 gen45_idol_48은 코드 무변경(편집기 입력값).
6. **EVENT_SPEC v1.1 수령·박제 후 계측 구현을 그 문서와 대조**(대표에게 파일 요청 중).
