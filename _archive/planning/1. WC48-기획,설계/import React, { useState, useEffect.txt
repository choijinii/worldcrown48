import React, { useState, useEffect } from 'react';
import { 
  Trophy, 
  LayoutDashboard, 
  PlusCircle, 
  Globe, 
  Share2, 
  Settings, 
  LogOut, 
  Search, 
  ChevronRight, 
  Camera, 
  Shield, 
  TrendingUp,
  Image as ImageIcon,
  CheckCircle2,
  Lock,
  Eye,
  Flame,
  Zap,
  Star,
  Users,
  ChevronLeft,
  Crown
} from 'lucide-react';

// --- Design System Tokens (Global MZ Style) ---
const COLORS = {
  bg: 'bg-[#05070A]', // Pure deep midnight
  surface: 'bg-[#0E1217]', // Elevated surface
  surfaceHover: 'hover:bg-[#161B22]',
  primary: 'text-[#FFD700]', // Pure Gold
  accent: 'bg-[#FFD700]',
  accentHover: 'hover:bg-[#FFC400]',
  textMain: 'text-slate-50',
  textMuted: 'text-slate-500',
  border: 'border-[#1E232B]',
};

const App = () => {
  // Domain Management State
  const [view, setView] = useState('PITCH'); // PITCH, LAB, ARENA, LOCKER
  const [user, setUser] = useState(null);
  const [category, setCategory] = useState('WORLD CUP');
  const [selectedTournament, setSelectedTournament] = useState(null);

  // --- Auth Logic ---
  const handleGoogleLogin = () => {
    setUser({ 
      id: 'user_8848',
      name: 'Alex Ferguson', 
      email: 'alex@globalfan.com', 
      image: 'https://api.dicebear.com/7.x/avataaars/svg?seed=Alex' 
    });
    setView('PITCH');
  };

  // --- Navigation Component ---
  const Navbar = () => (
    <nav className={`fixed top-0 w-full z-50 border-b ${COLORS.border} ${COLORS.surface} backdrop-blur-md bg-opacity-80 px-8 py-4 flex justify-between items-center`}>
      <div className="flex items-center gap-3 cursor-pointer group" onClick={() => setView('PITCH')}>
        <div className="bg-amber-500 p-1.5 rounded-lg group-hover:rotate-12 transition-transform">
          <Trophy className="text-black w-6 h-6" />
        </div>
        <span className="text-2xl font-black tracking-tighter text-white italic">WORLD CROWN <span className="text-amber-500 underline decoration-2 underline-offset-4">48</span></span>
      </div>
      
      <div className="hidden md:flex items-center gap-8">
        {['PITCH', 'LAB', 'LOCKER ROOM'].map((item) => (
          <button 
            key={item}
            onClick={() => {
              if (item === 'LOCKER ROOM') user ? setView('LOCKER') : handleGoogleLogin();
              else setView(item === 'LAB' ? 'LAB' : 'PITCH');
            }}
            className={`text-xs font-black tracking-widest transition-all ${
              (view === item || (view === 'LOCKER' && item === 'LOCKER ROOM')) ? 'text-amber-500' : 'text-slate-500 hover:text-white'
            }`}
          >
            {item}
          </button>
        ))}
      </div>

      <div className="flex items-center gap-4">
        {!user ? (
          <button 
            onClick={handleGoogleLogin}
            className="bg-white text-black px-5 py-2 rounded-full text-xs font-black flex items-center gap-2 hover:scale-105 transition-all"
          >
            <img src="https://www.google.com/favicon.ico" alt="G" className="w-3 h-3" />
            SIGN IN
          </button>
        ) : (
          <div className="flex items-center gap-3 cursor-pointer" onClick={() => setView('LOCKER')}>
            <div className="text-right hidden sm:block">
              <p className="text-[10px] font-black text-white leading-none">{user.name}</p>
              <p className="text-[9px] text-amber-500 font-bold uppercase tracking-tighter">Pro Fan</p>
            </div>
            <img src={user.image} alt="user" className="w-9 h-9 rounded-full border-2 border-amber-500 p-0.5" />
          </div>
        )}
      </div>
    </nav>
  );

  // --- Domain 1: The Pitch (Landing & Discovery) ---
  const PitchDomain = () => (
    <div className="pt-32 px-8 max-w-7xl mx-auto pb-32 animate-in fade-in duration-700">
      <header className="relative mb-20">
        <div className="absolute -top-20 -left-20 w-96 h-96 bg-amber-500/10 blur-[120px] rounded-full" />
        <div className="relative z-10">
          <div className="flex items-center gap-2 text-amber-500 mb-4 animate-bounce">
            <Flame size={20} fill="currentColor" />
            <span className="text-sm font-black tracking-[0.3em] uppercase">The World is Watching</span>
          </div>
          <h1 className="text-6xl md:text-8xl font-black mb-8 leading-[0.9] tracking-tighter uppercase italic">
            Who wears the <br />
            <span className="text-amber-500">Ultimate Crown?</span>
          </h1>
          <p className="text-slate-400 text-lg max-w-xl font-medium mb-10 leading-relaxed">
            48 Contenders. 1:1 Knockouts. Only one survivor. <br />
            Create or join the most intense fan tournament on Earth.
          </p>
          <div className="flex flex-wrap gap-4">
            <button onClick={() => setView('LAB')} className="bg-amber-500 text-black px-10 py-5 rounded-2xl font-black text-lg hover:scale-105 transition-all flex items-center gap-3">
              <PlusCircle size={24} /> CREATE YOUR 48
            </button>
            <button className="bg-slate-800 text-white px-10 py-5 rounded-2xl font-black text-lg hover:bg-slate-700 transition-all border border-slate-700">
              EXPLORE PITCH
            </button>
          </div>
        </div>
      </header>

      {/* Category Modules */}
      <div className="grid grid-cols-1 lg:grid-cols-3 gap-12">
        <div className="lg:col-span-2 space-y-12">
          {/* Module: Trending */}
          <section>
            <div className="flex justify-between items-end mb-6">
              <h2 className="text-2xl font-black italic flex items-center gap-2">
                <Zap className="text-amber-500" fill="currentColor" /> TRENDING PITCHES
              </h2>
              <div className="flex gap-2">
                {['WORLD CUP', 'K-POP'].map(cat => (
                  <button 
                    key={cat} 
                    onClick={() => setCategory(cat)}
                    className={`text-[10px] font-black px-4 py-1.5 rounded-full border ${category === cat ? 'bg-white text-black border-white' : 'text-slate-500 border-slate-800 hover:border-slate-600'}`}
                  >
                    {cat}
                  </button>
                ))}
              </div>
            </div>
            <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
              {[1, 2, 3, 4].map(i => (
                <div key={i} onClick={() => setView('ARENA')} className={`${COLORS.surface} ${COLORS.surfaceHover} p-5 rounded-3xl border ${COLORS.border} transition-all cursor-pointer group border-l-4 border-l-transparent hover:border-l-amber-500`}>
                  <div className="flex justify-between items-start mb-4">
                    <span className="text-[10px] font-bold text-amber-500 bg-amber-500/10 px-2 py-1 rounded tracking-tighter">LIVE 48</span>
                    <span className="text-[10px] font-mono text-slate-500">#{1024 + i}</span>
                  </div>
                  <h3 className="text-lg font-black mb-1 group-hover:text-amber-500 transition-colors uppercase">2026 World Cup: Top Strikers</h3>
                  <div className="flex items-center gap-4 text-[10px] font-bold text-slate-500">
                    <span className="flex items-center gap-1"><Users size={12}/> 12.4k Fans</span>
                    <span className="flex items-center gap-1"><Star size={12}/> 98% Hype</span>
                  </div>
                </div>
              ))}
            </div>
          </section>
        </div>

        {/* Module: Leaderboard */}
        <aside className={`${COLORS.surface} border ${COLORS.border} rounded-[40px] p-8 h-fit sticky top-32`}>
          <h2 className="text-xl font-black italic mb-8 flex items-center gap-2">
            <Trophy className="text-amber-500" /> TOP CREATORS
          </h2>
          <div className="space-y-6">
            {[1, 2, 3, 4, 5].map(rank => (
              <div key={rank} className="flex items-center gap-4">
                <span className="text-lg font-black italic text-slate-700 w-6">0{rank}</span>
                <div className="w-10 h-10 rounded-full bg-slate-800 overflow-hidden border border-slate-700">
                  <img src={`https://api.dicebear.com/7.x/avataaars/svg?seed=User${rank}`} alt="" />
                </div>
                <div className="flex-1">
                  <p className="text-xs font-black uppercase">Creator_{rank}48</p>
                  <p className="text-[10px] font-mono text-amber-500/70">Score: {10000 - rank * 1200}</p>
                </div>
                {rank === 1 && <Crown size={16} className="text-amber-500" fill="currentColor" />}
              </div>
            ))}
          </div>
          <button className="w-full mt-10 py-4 bg-slate-800 hover:bg-slate-700 rounded-2xl text-[10px] font-black tracking-widest transition-all">VIEW FULL RANKINGS</button>
        </aside>
      </div>
    </div>
  );

  // --- Domain 2: The Lab (Creation & AI) ---
  const LabDomain = () => {
    const [step, setStep] = useState(1);
    const [title, setTitle] = useState('');
    
    return (
      <div className="pt-32 px-8 max-w-5xl mx-auto pb-32 animate-in slide-in-from-bottom-4 duration-500">
        <div className="flex flex-col md:flex-row justify-between items-end gap-6 mb-12">
          <div>
            <h2 className="text-4xl font-black italic uppercase">The Lab</h2>
            <p className="text-slate-500 mt-2 font-medium">Constructing your 48-candidate battlefield.</p>
          </div>
          <div className="flex gap-3">
            <div className="flex items-center gap-2 px-4 py-2 bg-slate-900 border border-slate-800 rounded-full">
              <div className={`w-2 h-2 rounded-full ${step >= 1 ? 'bg-amber-500' : 'bg-slate-700'}`} />
              <div className={`w-2 h-2 rounded-full ${step >= 2 ? 'bg-amber-500' : 'bg-slate-700'}`} />
              <div className="text-[10px] font-black text-slate-500 ml-2 uppercase">Step 0{step}</div>
            </div>
          </div>
        </div>

        <div className={`${COLORS.surface} border ${COLORS.border} rounded-[48px] p-10 shadow-2xl`}>
          {step === 1 ? (
            <div className="space-y-10">
              <div>
                <label className="text-[10px] font-black text-amber-500 uppercase tracking-[0.2em] mb-4 block">Tournament Identity</label>
                <input 
                  type="text" 
                  value={title}
                  onChange={(e) => setTitle(e.target.value)}
                  placeholder="ENTER TOURNAMENT NAME (e.g. Best K-Pop Dancer)" 
                  className="w-full bg-transparent border-b-2 border-slate-800 py-4 text-3xl font-black uppercase placeholder:text-slate-800 focus:border-amber-500 outline-none transition-all"
                />
              </div>

              <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div className="p-6 bg-slate-900/50 rounded-3xl border border-slate-800 hover:border-amber-500/30 transition-all cursor-pointer group">
                  <div className="w-12 h-12 bg-amber-500 rounded-2xl flex items-center justify-center mb-6 group-hover:rotate-6 transition-transform">
                    <Zap className="text-black" fill="currentColor" />
                  </div>
                  <h4 className="text-lg font-black uppercase mb-2">AI Quick-Collect</h4>
                  <p className="text-xs text-slate-500 leading-relaxed">Our AI will scout the web to gather 48 high-quality images and metadata based on your title.</p>
                </div>
                <div className="p-6 bg-slate-900/50 rounded-3xl border border-slate-800 hover:border-amber-500/30 transition-all cursor-pointer group">
                  <div className="w-12 h-12 bg-slate-800 rounded-2xl flex items-center justify-center mb-6 group-hover:rotate-6 transition-transform text-white">
                    <ImageIcon size={24} />
                  </div>
                  <h4 className="text-lg font-black uppercase mb-2">Manual Curation</h4>
                  <p className="text-xs text-slate-500 leading-relaxed">Perfect for creators who want absolute control. Upload and name all 48 candidates yourself.</p>
                </div>
              </div>

              <div className="pt-10 border-t border-slate-800 flex justify-end">
                <button 
                  disabled={!title}
                  onClick={() => setStep(2)}
                  className="bg-white text-black disabled:bg-slate-800 disabled:text-slate-600 px-12 py-4 rounded-2xl font-black transition-all hover:scale-105"
                >
                  INITIALIZE 48 NODES
                </button>
              </div>
            </div>
          ) : (
            <div className="space-y-8 text-center py-10">
              <div className="grid grid-cols-6 md:grid-cols-8 gap-3 max-h-[400px] overflow-y-auto pr-4 custom-scrollbar">
                {Array(48).fill(0).map((_, i) => (
                  <div key={i} className="aspect-square bg-slate-900 rounded-xl border border-slate-800 flex items-center justify-center relative group overflow-hidden cursor-pointer hover:border-amber-500">
                    <Camera size={16} className="text-slate-700 group-hover:text-amber-500" />
                    <span className="absolute bottom-1 right-1 text-[8px] font-black text-slate-800">{i+1}</span>
                  </div>
                ))}
              </div>
              <div className="flex justify-center gap-4 pt-10">
                <button onClick={() => setStep(1)} className="px-10 py-4 border border-slate-800 rounded-2xl font-black text-sm text-slate-500 hover:text-white">RE-PLAN</button>
                <button onClick={() => setView('LOCKER')} className="px-10 py-4 bg-amber-500 text-black rounded-2xl font-black text-sm hover:scale-105 transition-all">PUBLISH TO PITCH</button>
              </div>
            </div>
          )}
        </div>
      </div>
    );
  };

  // --- Domain 3: The Arena (Tournament Gameplay) ---
  const ArenaDomain = () => {
    return (
      <div className="pt-32 px-8 max-w-4xl mx-auto pb-32 text-center">
        <div className="mb-12">
          <span className="text-[10px] font-black text-amber-500 tracking-[0.4em] uppercase border border-amber-500 px-4 py-1 rounded-full mb-6 inline-block">Round of 48: Heat 01</span>
          <h2 className="text-4xl font-black uppercase italic">Battle for the Crown</h2>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 gap-4 relative">
          <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 z-10 bg-black border-2 border-amber-500 w-12 h-12 rounded-full flex items-center justify-center font-black italic text-amber-500 shadow-[0_0_20px_rgba(245,158,11,0.3)]">
            VS
          </div>
          
          <div className={`${COLORS.surface} border-2 border-transparent hover:border-amber-500 rounded-[40px] overflow-hidden cursor-pointer transition-all group`}>
            <div className="aspect-[4/5] bg-slate-800 relative">
               <div className="absolute inset-0 bg-gradient-to-t from-black via-transparent to-transparent opacity-60" />
               <div className="absolute bottom-8 left-0 right-0 p-6 text-center">
                 <p className="text-xs font-bold text-amber-500 uppercase tracking-widest mb-1">BRAZIL</p>
                 <h4 className="text-2xl font-black uppercase italic group-hover:scale-110 transition-transform">Vinícius Jr.</h4>
               </div>
            </div>
          </div>

          <div className={`${COLORS.surface} border-2 border-transparent hover:border-amber-500 rounded-[40px] overflow-hidden cursor-pointer transition-all group`}>
            <div className="aspect-[4/5] bg-slate-800 relative">
               <div className="absolute inset-0 bg-gradient-to-t from-black via-transparent to-transparent opacity-60" />
               <div className="absolute bottom-8 left-0 right-0 p-6 text-center">
                 <p className="text-xs font-bold text-amber-500 uppercase tracking-widest mb-1">FRANCE</p>
                 <h4 className="text-2xl font-black uppercase italic group-hover:scale-110 transition-transform">Kylian Mbappé</h4>
               </div>
            </div>
          </div>
        </div>

        <div className="mt-16 flex justify-center gap-2">
          {Array(10).fill(0).map((_, i) => (
            <div key={i} className={`h-1 rounded-full ${i === 0 ? 'w-8 bg-amber-500' : 'w-4 bg-slate-800'}`} />
          ))}
        </div>
        <p className="mt-4 text-[10px] font-black text-slate-500 uppercase tracking-widest">Progress: 1/47 Matches</p>
      </div>
    );
  };

  // --- Domain 4: The Locker Room (User Dashboard) ---
  const LockerDomain = () => (
    <div className="pt-32 px-8 max-w-7xl mx-auto pb-32 animate-in fade-in slide-in-from-right-4 duration-500">
      <div className="grid grid-cols-1 lg:grid-cols-4 gap-12">
        {/* Profile Info */}
        <aside className="lg:col-span-1 space-y-8">
          <div className={`${COLORS.surface} border ${COLORS.border} rounded-[40px] p-8 text-center`}>
            <div className="relative inline-block mb-6">
              <img src={user?.image} alt="" className="w-32 h-32 rounded-full border-4 border-amber-500 mx-auto" />
              <div className="absolute bottom-0 right-0 bg-amber-500 text-black p-2 rounded-full border-4 border-[#0E1217]">
                <Settings size={16} />
              </div>
            </div>
            <h3 className="text-2xl font-black uppercase italic">{user?.name}</h3>
            <p className="text-xs text-slate-500 font-mono mt-1 mb-8">GLOBAL RANK: #8,241</p>
            
            <div className="grid grid-cols-2 gap-3">
              <div className="bg-slate-900 p-4 rounded-2xl">
                <p className="text-xl font-black text-amber-500">14</p>
                <p className="text-[8px] font-black text-slate-500 uppercase">Created</p>
              </div>
              <div className="bg-slate-900 p-4 rounded-2xl">
                <p className="text-xl font-black text-amber-500">892</p>
                <p className="text-[8px] font-black text-slate-500 uppercase">Voted</p>
              </div>
            </div>
          </div>

          <div className="space-y-4">
             <button onClick={() => setView('PITCH')} className="w-full flex items-center justify-between p-5 bg-slate-900 hover:bg-slate-800 rounded-2xl transition-all group">
               <span className="text-xs font-black uppercase tracking-widest">Back to Pitch</span>
               <ChevronRight size={16} className="text-slate-600 group-hover:text-amber-500" />
             </button>
             <button onClick={() => setUser(null)} className="w-full flex items-center justify-between p-5 bg-red-500/5 hover:bg-red-500/10 rounded-2xl transition-all group border border-red-500/10">
               <span className="text-xs font-black uppercase tracking-widest text-red-500">Sign Out</span>
               <LogOut size={16} className="text-red-500 opacity-50" />
             </button>
          </div>
        </aside>

        {/* Content Tabs */}
        <div className="lg:col-span-3 space-y-12">
          <section>
            <div className="flex justify-between items-end border-b border-slate-800 pb-4 mb-8">
              <h2 className="text-xl font-black italic uppercase">My 48 Projects</h2>
              <div className="flex gap-6 text-[10px] font-black text-slate-500 uppercase tracking-widest">
                <button className="text-amber-500 border-b-2 border-amber-500 pb-4 -mb-[18px]">All Creations</button>
                <button className="hover:text-white transition-colors pb-4">Drafts</button>
              </div>
            </div>

            <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
              {[1, 2, 3].map(i => (
                <div key={i} className={`${COLORS.surface} border ${COLORS.border} p-6 rounded-[32px] group hover:border-amber-500/30 transition-all`}>
                  <div className="flex justify-between items-start mb-6">
                    <div className="w-16 h-16 bg-slate-800 rounded-2xl" />
                    <div className="flex gap-2">
                       <button className="p-2 bg-slate-900 rounded-lg text-slate-500 hover:text-white"><Settings size={18}/></button>
                       <button className="p-2 bg-slate-900 rounded-lg text-slate-500 hover:text-amber-500"><Share2 size={18}/></button>
                    </div>
                  </div>
                  <h3 className="text-lg font-black uppercase mb-1">Hottest K-Pop Rookies 2026</h3>
                  <div className="flex items-center gap-4 text-[10px] font-bold text-slate-500 uppercase">
                    <span className="flex items-center gap-1 text-amber-500/80"><Eye size={12}/> Public</span>
                    <span className="flex items-center gap-1"><Users size={12}/> 1,204 Hits</span>
                  </div>
                </div>
              ))}
              <div onClick={() => setView('LAB')} className="border-2 border-dashed border-slate-800 rounded-[32px] flex flex-col items-center justify-center p-12 text-slate-700 hover:border-amber-500 hover:text-amber-500 cursor-pointer transition-all group">
                <PlusCircle size={40} className="mb-4 group-hover:scale-110 transition-transform" />
                <span className="text-xs font-black uppercase tracking-widest">Launch New 48</span>
              </div>
            </div>
          </section>
        </div>
      </div>
    </div>
  );

  return (
    <div className={`min-h-screen ${COLORS.bg} ${COLORS.textMain} font-sans selection:bg-amber-500 selection:text-black overflow-x-hidden`}>
      <Navbar />
      
      <main>
        {view === 'PITCH' && <PitchDomain />}
        {view === 'LAB' && <LabDomain />}
        {view === 'ARENA' && <ArenaDomain />}
        {view === 'LOCKER' && <LockerDomain />}
      </main>

      {/* Persistent Global Footer */}
      <footer className={`border-t ${COLORS.border} py-20 px-8 mt-20 relative overflow-hidden`}>
        <div className="absolute top-0 right-0 w-64 h-64 bg-amber-500/5 blur-[100px]" />
        <div className="max-w-7xl mx-auto flex flex-col md:flex-row justify-between items-center gap-12 relative z-10">
          <div className="text-center md:text-left">
            <div className="flex items-center justify-center md:justify-start gap-2 mb-4">
              <Trophy className="text-amber-500 w-5 h-5" />
              <span className="text-lg font-black tracking-tighter italic">WORLD CROWN 48</span>
            </div>
            <p className="text-[10px] text-slate-600 font-bold uppercase tracking-[0.3em] leading-relaxed">
              Global Fandom Data Hub <br /> Powered by AI Intelligence
            </p>
          </div>
          
          <div className="flex gap-12 text-[10px] font-black text-slate-500 uppercase tracking-widest">
             <div className="space-y-4">
               <p className="text-white mb-6">DOMAINS</p>
               <a href="#" className="block hover:text-amber-500">The Pitch</a>
               <a href="#" className="block hover:text-amber-500">The Lab</a>
               <a href="#" className="block hover:text-amber-500">The Arena</a>
             </div>
             <div className="space-y-4">
               <p className="text-white mb-6">LEGAL</p>
               <a href="#" className="block hover:text-amber-500">Privacy</a>
               <a href="#" className="block hover:text-amber-500">Terms</a>
               <a href="#" className="block hover:text-amber-500">Cookie Policy</a>
             </div>
          </div>
        </div>
        <div className="max-w-7xl mx-auto mt-20 pt-8 border-t border-slate-900 flex justify-between items-center">
          <p className="text-[9px] font-mono text-slate-700 uppercase">Ver 1.0.48 Beta Phase</p>
          <div className="flex gap-4">
            <Share2 size={14} className="text-slate-700 hover:text-white cursor-pointer" />
            <Globe size={14} className="text-slate-700 hover:text-white cursor-pointer" />
          </div>
        </div>
      </footer>

      {/* Custom Styles */}
      <style dangerouslySetInnerHTML={{ __html: `
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&family=Playfair+Display:ital,wght@1,900&display=swap');
        
        .font-serif { font-family: 'Playfair Display', serif; }
        .font-sans { font-family: 'Inter', sans-serif; }

        .custom-scrollbar::-webkit-scrollbar { width: 4px; }
        .custom-scrollbar::-webkit-scrollbar-track { background: transparent; }
        .custom-scrollbar::-webkit-scrollbar-thumb { background: #1E232B; border-radius: 10px; }
        .custom-scrollbar::-webkit-scrollbar-thumb:hover { background: #FFD700; }

        @keyframes fade-in { from { opacity: 0; } to { opacity: 1; } }
        .animate-in { animation: fade-in 0.5s ease-out forwards; }
      `}} />
    </div>
  );
};

export default App;