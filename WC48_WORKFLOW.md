# WorldCrown48 (월크48) — 작업 워크플로우
# WC48_WORKFLOW.md v1.0 — 2026-05-14 작성 | 작성자: 48티오

> **이 파일의 목적**: 월크48 프로젝트의 **모든 작업 순서·의사결정·범위 판단**에 대한 단일 진실 공급원(Single Source of Truth)입니다.
> CONTEXT.md §작업 원칙에서 "추측·임의 처리 절대 금지"를 선언하고, 이 파일이 그 구체적 실행 규칙을 정의합니다.

---

## 🚨 절대 원칙

```
1. 자산 없이 시안 없다 — 등록된 자산만 사용한다
2. 단계를 건너뛰지 않는다 — 자산 → 시스템 → 검증 → 구현
3. 마스터 문서 변경은 사전 협의 — v4.9 설계서를 변경할 일이라면 멈춘다
4. 의문은 즉시 보고한다 — 추측으로 진행하지 않는다
```

---

## 📐 4단계 표준 워크플로우

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  ① 자산 등록     │ → │  ② 시스템 정의   │ → │  ③ 시안 검증     │ → │  ④ 코드 구현     │
│  (Asset)         │    │  (System)        │    │  (Validation)    │    │  (Implementation)│
└─────────────────┘    └─────────────────┘    └─────────────────┘    └─────────────────┘
```

### ① 자산 등록 (Asset Registration)

**무엇을 등록하나**

| 자산 종류 | 예시 | 저장 위치 |
|---|---|---|
| 브랜드 자산 | Crown 로고 SVG, 워드마크 SVG, 파비콘 | `/public/brand/` |
| 콘텐츠 자산 | Contestant 이미지, Tournament 썸네일 | Firebase Storage `contestants/` |
| 폰트 | Inter, Pretendard, Playfair Display, JetBrains Mono | Google Fonts CDN 또는 `/public/fonts/` |
| 아이콘 | Lucide React (기본), 커스텀 SVG 아이콘 | `npm: lucide-react` + `/public/icons/` |
| 색상 토큰 | Tailwind config의 컬러 변수 | `tailwind.config.ts` |
| 데이터 시드 | Contestant 샘플 데이터, 카테고리 마스터 | `/data/seed/` |

**등록 완료 조건**

```
✅ 파일이 정해진 경로에 존재한다
✅ 파일명이 명명 규칙을 따른다 (WC48_SVG_GUIDE.md 참조)
✅ 자산 인벤토리 문서에 기재되어 있다
✅ 라이선스가 명확하다 (CC0 / MIT / 자체 제작 등)
```

**❌ 자산이 없을 때 절대 하지 말 것**

```
- 임의로 그려서 진행 (Claude 또는 사람 모두)
- "곧 등록될 것이다" 가정하고 자리 차지용 더미 자산 만들기
- 비슷한 자산을 임시로 가져다 쓰기
- 텍스트나 이모지로 대체하기

자산이 없으면 "자산 등록 필요" 보고로 작업을 멈춘다.
```

### ② 시스템 정의 (System Definition)

등록된 자산을 어떻게 사용할지 시스템·명세서에 반영하는 단계.

**산출물**

```
- WC48_DESIGN_SYSTEM_v2.md  : 컬러·타이포·간격·그림자 토큰
- v4.9 설계서                : 도메인·모듈·데이터 구조 (★ 단일 진실 공급원)
- LANGUAGE.md v1.2          : 공식 용어 (★ 단일 진실 공급원)
- 컴포넌트 명세              : Props 인터페이스·상태 다이어그램
- API 명세                   : Cloud Functions 시그니처
- Firestore 스키마          : 컬렉션·문서 구조
```

**시스템 정의의 범위 한계 — 절대 위반 금지**

```
시스템 문서는 "어떻게 칠할 것인가" "어떻게 부를 것인가" "어떤 구조인가"를 정의한다.
✅ 디자인 시스템: 컬러·타이포·간격
✅ 용어집:        명사·동사의 공식 호칭
✅ 설계서:        도메인·모듈·데이터 구조

❌ 시스템 문서가 자산을 직접 묘사하지 않는다
   - "Crown 로고는 □□□ 모양"이라고 모양을 글로 묘사 금지
   - "Champion 이미지는 △△△ 스타일"이라고 스타일을 글로 묘사 금지
   - 대신: "등록된 wc48-crown-filled.svg 사용"이라고 자산 파일을 가리킨다
```

### ③ 시안 검증 (Validation)

코드를 짜기 **전에**, 디자인 툴에서 실제 화면 시안을 만들어 검증한다.

**검증 도구**

| 도구 | 용도 | 비용 |
|---|---|---|
| Figma | UI 시안·디자인 토큰·컴포넌트 | Free Tier 가능 |
| Stitch (Google) | AI 기반 UI 시안 생성 | Free Tier 가능 |
| Storybook | 컴포넌트 단위 시각 검증 | OSS 무료 |
| HTML 프로토타입 | 빠른 인터랙션 검증 | 무료 |

**검증 통과 기준**

```
✅ 모바일(375px) / 태블릿(768px) / 데스크탑(1440px) 3화면 시안 완성
✅ 등록된 자산만 사용 (임의로 그린 요소 없음)
✅ 디자인 시스템 v2 컬러 토큰만 사용
✅ 대표님 시각 검토 통과
✅ 접근성 대비비 기준 충족 (텍스트 4.5:1 이상)
```

**검증을 건너뛰면 안 되는 이유**

```
"코드로 만들면서 보면 된다"는 오류:
- 코드 작성은 디자인 검증의 5~10배 시간 소요
- 시각 문제는 코드에서 발견하면 재작업 비용이 크다
- Claude Code는 시안을 따라 만드는 것이지 시안 자체를 만들지 않는다
```

### ④ 코드 구현 (Implementation)

검증된 시안을 코드로 옮긴다.

**구현 순서**

```
1. 컴포넌트 (Atomic Design: Atom → Molecule → Organism → Template → Page)
2. 단일 컴포넌트 단위 Storybook 검증
3. 페이지 통합
4. E2E 시나리오 테스트
5. 배포 (Vercel Preview → 검토 → Production)
```

**구현 단계에서 절대 하지 말 것**

```
❌ 시안에 없는 요소를 코드에서 추가
❌ 시안과 다른 컬러·간격을 임의로 적용
❌ 디자인 토큰 외 하드코딩된 색상값 사용
❌ TypeScript 타입 무시 (any 남발 금지)
❌ 등록되지 않은 라이브러리 임의 추가 (npm install 전 ADR 작성)
```

---

## 🎯 의사결정 트리: "이 작업을 진행해도 되는가?"

작업 시작 전 아래 트리를 따라 자문합니다.

```
[1] 이 작업이 요청 범위 안의 일인가?
    ├─ Yes → [2]로
    └─ No  → ❌ 멈추고 대표님께 보고: "요청 범위 밖입니다"

[2] 등록된 자산만 사용하는가?
    ├─ Yes → [3]으로
    └─ No  → ❌ 멈추고 보고: "자산 등록 필요 — [자산명] 등록 후 진행"

[3] 이 작업이 v4.9 설계서·LANGUAGE.md·CONTEXT.md 변경을 유발하는가?
    ├─ Yes → ❌ 멈추고 보고: "마스터 문서 변경 필요 — 사전 협의 요청"
    └─ No  → [4]로

[4] 디자인 시안 검증을 거쳤는가? (코드 작업의 경우)
    ├─ Yes → [5]로
    └─ No  → ❌ 멈추고: "Figma·Stitch에서 시안 검증 먼저"

[5] 의문이 있는 부분이 남아 있는가?
    ├─ Yes → ❌ 멈추고 보고: "의문 사항 — [내용] 확인 요청"
    └─ No  → ✅ 진행
```

---

## 📋 작업 유형별 절차

### A. 새 화면(페이지) 제작

```
① 자산 확인
   - 화면에 사용될 SVG·이미지·아이콘이 모두 등록되어 있는가?
   - Tailwind 컬러 토큰이 정의되어 있는가?

② 시스템 확인
   - v4.9 설계서에서 해당 도메인·모듈 명세 확인
   - LANGUAGE.md에서 화면명·요소명 공식 용어 확인
   - 디자인 시스템 v2에서 적용 컬러·타이포 확인

③ 시안 검증
   - Figma 또는 Stitch에서 3화면(모바일·태블릿·데스크탑) 시안 작성
   - 대표님 검토 통과

④ 코드 구현
   - Next.js App Router 페이지 작성
   - 컴포넌트 분리 (Atomic Design)
   - Storybook 검증
   - 통합 후 배포
```

### B. 새 컴포넌트 제작

```
① 자산 확인 — 필요한 아이콘·이미지 등록 여부
② 시스템 확인 — Props 인터페이스가 v4.9 설계서 또는 컴포넌트 명세에 정의되어 있는가?
③ 시안 검증 — Figma에서 컴포넌트 변형(state, variant) 시각화
④ 구현 — Storybook 우선 작성 → 페이지 통합
```

### C. 기존 화면 컬러 변경

```
① 자산 확인 — 새 컬러 토큰이 디자인 시스템 v2에 정의되어 있는가?
② 시스템 확인 — Tailwind config에 토큰 반영되어 있는가?
③ 시안 검증 — 변경 전후 비교 시안 작성
④ 코드 변경 — 클래스명 또는 변수만 교체 (구조 변경 없음)
```

### D. 새 기능 추가 (DB·API·로직 변경)

```
⚠️ 이 작업은 v4.9 설계서 변경을 유발할 가능성이 높음.

① 사전 보고: "v4.9 설계서 변경 필요 가능성 있음" 대표님께 보고
② 승인 받은 후에만 진행
③ ADR(Architecture Decision Record) 작성: 변경 사유·영향 범위·대안
④ v4.9 설계서 갱신 (v5.0으로)
⑤ 자산·시스템·시안·구현 순서 진행
```

### E. 디자인 시스템 자체 변경 (이번 v2 같은 경우)

```
① 변경 요청 범위 명시 — "어떤 부분을 어떻게 바꾸는가" 문서화
② 마스터 문서(v4.9) 변경 영향 검토 — 도메인·모듈·데이터 변경 유발 여부
   - Yes → §D 절차 진행 (사전 협의)
   - No  → 디자인 시스템 문서만 갱신
③ 새 버전 파일 생성 (예: v1 → v2)
   - 기존 버전은 보존
   - 변경 이력 명시
④ 시안 검증 — 변경된 시스템을 실제 화면에 적용
⑤ 단계적 코드 반영
```

---

## 🚦 상황별 행동 규칙

### 상황 1: 자산이 등록되어 있지 않은데 시안 요청이 들어왔다

```
❌ 절대 하지 말 것: 비슷한 자산을 임의로 그려서 시안 만들기
✅ 올바른 행동: "자산 [이름]이 등록되지 않았습니다. 등록 후 시안 작성 가능합니다." 보고
```

### 상황 2: 작업 중에 "이걸 추가하면 더 좋을 것 같다"는 아이디어가 떠올랐다

```
❌ 절대 하지 말 것: 작업에 슬쩍 끼워 넣기
✅ 올바른 행동: 본 작업은 요청 범위로 완료. 별도 제안서로 분리하여 대표님께 보고.
   "이런 아이디어가 있는데, 검토해 보시겠어요?" 형식.
```

### 상황 3: 작업이 마스터 문서 변경을 유발한다는 것을 작업 중에 발견했다

```
❌ 절대 하지 말 것: 작업을 계속 진행하고 나중에 한꺼번에 보고
✅ 올바른 행동: 즉시 멈추고 보고. "이 작업은 v4.9 설계서 [섹션] 변경을 유발합니다. 진행 여부 확인 부탁드립니다."
```

### 상황 4: 대표님 요청이 모호하거나 추측이 필요해 보인다

```
❌ 절대 하지 말 것: 가장 그럴듯한 해석으로 진행
✅ 올바른 행동: "이 부분의 의도가 [A]인지 [B]인지 확인 부탁드립니다." 보고 후 진행
```

### 상황 5: Claude/AI 에이전트가 기억하지 못하는 과거 합의가 있다는 느낌이 든다

```
❌ 절대 하지 말 것: 추측으로 새 합의안 만들기
✅ 올바른 행동: conversation_search 또는 project_knowledge_search 도구로 과거 문서 확인.
   못 찾으면: "이 부분에 대한 과거 합의 내용을 확인할 수 없습니다. 다시 알려주실 수 있나요?"
```

---

## 📁 문서 우선순위 (충돌 시 적용 순서)

같은 주제에 대해 여러 문서가 다른 내용을 말한다면, 아래 순서로 우선합니다.

```
1순위: LANGUAGE.md v1.2           — 용어 정의 (절대 우선)
2순위: WorldCrown48_v4_9.md        — 도메인·모듈·데이터 구조
3순위: CONTEXT_v0_6.md             — 작업 원칙·정책 요약
4순위: WC48_WORKFLOW.md (이 파일)  — 작업 순서·절차
5순위: WC48_DESIGN_SYSTEM_v2.3.md  — 컬러·타이포 토큰
6순위: WC48_SVG_GUIDE.md           — SVG 제작 사양
7순위: ProjectSkill (Claude 에이전트 가이드)
8순위: 기타 보조 문서
```

충돌 발견 시 즉시 보고합니다. 임의로 한쪽을 따르지 않습니다.

---

## ✅ 작업 시작 전 체크리스트 (모든 작업 공통)

```
□ 요청 내용을 정확히 이해했는가? (모호한 부분은 먼저 질문)
□ 이 작업이 어느 단계에 해당하는가? (자산/시스템/시안/구현)
□ 필요한 자산이 모두 등록되어 있는가?
□ 참조해야 할 마스터 문서를 확인했는가?
□ 이 작업이 다른 마스터 문서의 변경을 유발하는가? (Yes면 멈춤)
□ 의문 사항이 남아 있는가? (Yes면 멈춤)
□ 작업 결과물의 산출물·저장 위치·이름이 명확한가?
```

---

## 📝 작업 완료 후 체크리스트

```
□ 산출물이 정해진 위치에 저장되었는가?
□ 파일명이 명명 규칙을 따르는가?
□ 변경 이력이 기록되었는가? (변경 이력 표 갱신)
□ 영향 받는 다른 문서가 있다면 동기화되었는가?
□ 대표님께 보고할 요약이 준비되었는가?
□ 다음 단계 추천이 명시되었는가?
```

---

## 변경 이력

| 버전 | 날짜 | 주요 변경 |
| --- | --- | --- |
| **v1.0** | **2026-05-14** | **★ 최초 작성 — 디자인 시스템 v2 작업 중 발생한 추측·임의 처리 실수를 계기로 작업 워크플로우 단일 진실 공급원 신설** |

---

*© 2026 WorldCrown48 | 작성: 48티오 | WC48_WORKFLOW.md v1.0 | CONFIDENTIAL*
